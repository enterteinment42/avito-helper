# Авито-помощник (avito-helper)

Отдельное веб-приложение в рамках проекта «Поиграем» — полуавтоматизация размещения объявлений на Авито для продажи игр и подписок.

## Текущий фокус

**Этап:** Активная эксплуатация. Инструмент используется.
**Следующий шаг:** 🆕 Сессия 2026-07-11/12 — реализована и задеплоена **переработка «рандомных заголовков»** из аудита Fable 5 (коммиты `595622a` + `8289461` хардинг) — **визуально НЕ проверена**. С учётом F1-F8 (закоммичены ранее: `b1756d0` F1/F3/F4, `0a31030` F2/F5/F6/F7/F8) — **ВЕСЬ аудит Fable 5 теперь реализован в КОДЕ, но НИ ОДИН пункт не прошёл визуальную приёмку на живом инструменте**. Приоритет след. сессии — прогнать всё глазами. Ключевое для заголовков: реальная генерация → шаг «🏷️ Сначала заголовки» даёт 8-12 названий на выбор → отметить несколько → «Описания для выбранных →» приходят с ТОЧНО выбранными названиями (хардинг гарантирует), проверить что новые заголовки чище прежних «рандомных слов». ⚠️ Дрейф: запись лога «сессия 2 — F2/F5/F6/F7/F8» потеряна с диска (код закоммичен, лог откатился rebase-ом параллельной сессии) — восстановить, если Денис подтвердит. Прежние хвосты: каталоги реальными играми → цены на подписки; этапы 3–4 GFX-аудита (GFX-8…14), UX-9…15, старые баги #5–#8 (Bug #7 отчасти неактуален).
**Блокеры:** нет
**Открытые вопросы:** актуальные цены на подписки; точный размер фото на Авито; объём этапов 3–4 GFX-аудита (тач/персистентность/усиление вариантов) не согласован с Денисом; восстанавливать ли потерянную запись лога «сессия 2 F2-F8»
**Настроено:** git push напрямую из Claude Code через `git.exe` из GitHub Desktop (`C:\Users\enter\AppData\Local\GitHubDesktop\app-<версия>\resources\app\git\cmd\git.exe` — версия меняется с обновлениями GitHub Desktop, на 2026-07-10 это `app-3.6.1`; искать актуальную папку `app-*`)
**Эндпоинты на VPS:** `/igdb/img` (прокси IGDB + PS Store, задеплоен) ⚠️ `/api/openrouter` (прокси OpenRouter) — числился задеплоенным, но по факту ОТСУТСТВУЕТ на живом сервере и в истории git, см. Bug #9 ниже. `/api/claude` теперь (2026-07-12, root-проект SEC-1) требует `Authorization: Bearer <ADMIN_TOKEN>` — `getProxyToken()` в `avito-helper.html` уже это учитывает, при первом запросе спросит токен и запомнит в `localStorage['avito_admin_token']`.

**Известные баги (не исправлены):**
- **Bug #9 (найден 2026-07-12, из корневого аудита безопасности Fable, сессия 77 root-проекта)** — `/api/openrouter` числится в этом файле и в `SESSIONS-ARCHIVE.md` «✅ работает/задеплоен», но реально НЕ существует: ни в живом `~/poigraem-backend/server.js` на VPS, ни в единой истории git `backend/server.js` (~40 коммитов проверено, включая все безымянные «Update server.js») эндпоинта нет. Похоже на классический для этого проекта дрейф VPS↔репо — кто-то добавил его вручную на сервере без коммита в прошлой сессии, живой файл впоследствии где-то перезаписан, код потерян. Не блокирует основной сценарий — Claude остаётся провайдером по умолчанию, `getProxyToken()` (добавлен той же сессией 77 root-проекта, коммит `b79bd9d`) готовит `Authorization` для обеих веток `callAI`, но ветка OpenRouter упрётся в 404, пока эндпоинт не восстановлен. Решить: либо воссоздать `/api/openrouter` на VPS (текст реализации есть в `SESSIONS-ARCHIVE.md`, строка про добавление эндпоинта), либо временно убрать выбор провайдера OpenRouter из UI, пока его нет.
- **Bug #5** — `queue-add-batch` ставит `source:'fav'` с `favId:null`; работает случайно через fallback `item.variant`. Безвредно, но семантически неверно.
- **Bug #6** — `doRegen` не передаёт `{ webSearch }` в `callAI` → при перегенерации объявления для предзаказа не ищутся бонусы в сети (в отличие от первичной генерации `doGenerate`).
- **Bug #7** — мёртвый код базы данных: вкладка «База» удалена из UI, но `dbTabHTML`/`bindDbTab`/`renderDb` (~120 строк) остались в файле; `state.db` продолжает молча накапливаться в localStorage.
- **Bug #8** — раскладка `circ4d` недостижима: есть в `cvCells` и `cvLayoutSVG`, но отсутствует в `COLLAGE_UI_GROUPS` и `COLLAGE_LAYOUTS`.

## UI/UX аудит графики (обложки) — 2026-07-02

Сплошное чтение модуля `cv*` (строки ~1996–3443 + делегированные обработчики ~3983–4623) с точки зрения пользователя и конечной картинки. Нумерация GFX-N — для ссылок. Что работает хорошо и трогать не надо: математика всех 25+ раскладок (ячейки досчитываются до края, пустот нет), Круг+4/Квадрат+4 с кольцом и тенью, zoom/pan через `cvFitSrcRect` с клампом, drag-to-swap с подсветкой, авто-подгон текста стикера под форму, экспорт 1080×1350 JPEG 0.93.

✅ **Этапы 1+2 закрыты (сессия 2026-07-02 сессия 2, коммиты `8ef6039`+`a5d7e75`):** GFX-1, GFX-2, GFX-3, GFX-4, GFX-5, GFX-6, GFX-7 и фича GFX-15 — все мёртвые/обманчивые контролы + рамка картинки. Тексты и что именно исправлено — в `AUDITS-ARCHIVE.md` (команда `/session-audits-archive`). Ниже — только ещё открытые находки (этапы 3–4, объём не согласован с Денисом).

**🟠 UI обманывает / бесит:**
- **GFX-8.** Канвас — только mouse-события (~3289–3441), touch нет: на телефоне не работают drag стикера, drag текста, перестановка ячеек, сдвиг кадра игры (drag миниатюр — HTML5 DnD — на тач тоже мёртв). У «Текста на обложке» нет кнопочного fallback позиции → на мобиле надпись навсегда в центре. Подсказки при этом говорят «тяни мышкой».
- **GFX-9.** На мобиле (≤720px, `.cv-wrap` в одну колонку) превью оказывается внизу под всеми карточками настроек — крутишь слайдер и не видишь результат.
- **GFX-10.** Объект `cv` не входит в `save()` (~699) — все настройки обложек (текст/цвет/форма стикера, платформы, раскладка, формат) сбрасываются при F5.

**🟡 Качество конечного результата:**
- **GFX-11.** «✨ 4 варианта» слишком слабы для антидубля Авито: игра (~2750) — только фон/отступ/угол стикера, коллаж (~2783) — только фон/отступ; сама картинка пиксельно идентична, маскирующий стикер во всех 4 в одном месте одного цвета. Дешёвые оси, которых не хватает: зеркало, сдвиг кадра ±3% (transform уже есть), разные формы/позиции/цвета стикера по вариантам, warm/cool тон (уже реализован, в вариации не включён).
- **GFX-12.** Два конкурирующих стикера в режиме Игра: «Стикер с ценой» (4 угла, без размера/поворота/drag) функционально полностью покрывается «Маскирующим стикером» (7 форм, drag, поворот, размер). Дублирование с разными наборами настроек путает. Варианты: удалить простой стикер или дотянуть до общего уровня — решение за Денисом.
- **GFX-13.** Канвас не ждёт загрузки шрифтов: текст рисуется `'JetBrains Mono'`/`Literata` из Google Fonts без `document.fonts.ready` → при недоступном Google Fonts (РФ) текст молча уходит в системный fallback, обложки от сессии к сессии выглядят по-разному.
- **GFX-14.** Хитбоксы мыши: маскирующий стикер — квадрат `0.65×size` (~3286), а ribbon/badge шириной `1.5×size` — за края не ухватить; текст — `2×fontSize` (~3297), при размере 300 перехватывает пол-канваса и мешает сдвигу кадра в режиме Игра.

## Пользовательский аудит рабочего процесса (UX) — 2026-07-03

Сплошное чтение воронки продавца (формы генератора, `vcardHTML`/`conveyorHTML`/`refreshBatchResults`, очередь, сессия постинга, сканер, настройки) глазами продавца, чья цель — быстро постить десятки объявлений без банов за дубли. Нумерация UX-N. Сильные места (конвейер, пакет по регионам, сканер запрещёнки после багфиксов, детектор дублей, экспорт) — рабочие, не трогать. Главные пробелы — не в кнопках, а в **связности воронки**.

✅ **UX-1…UX-8 закрыты** (красные — сессия 2026-07-03, коммит `b87b877`; оранжевые — сессия 2026-07-03 сессия 2, коммит `af14b01`). Тексты находок и что исправлено — в `AUDITS-ARCHIVE.md` (команда `/session-audits-archive`). Ниже — только ещё открытая полировка.

**🟡 Полировка (НЕ исправлено):**
- **UX-9.** Нет счётчика длины названия при вводе в форме (только на результатах и в Сканере).
- **UX-10.** Деструктив без подтверждения: `queue-clear` (вся очередь), `batch-clear` (весь пакет) — без `confirm()`.
- **UX-11.** На мобиле (≤780px) после генерации результаты внизу под длинной формой, нет автоскролла к ним.
- **UX-12.** Весь UI зависит от Google Fonts (Literata + JetBrains Mono) — в РФ CDN нестабилен, интерфейс падает в системный fallback. Тот же корень, что GFX-13, но для всего приложения. Вариант — самохостинг шрифтов.
- **UX-13.** Порог жёлтого «N/50» (`lenClass`: жёлтый с 45, красный с 51) не объяснён.
- **UX-14.** Переключатель провайдера/модели спрятан за маленькой «⚙ AI» — Haiku для дешёвых черновиков трудно найти.
- **UX-15.** Нет «догенерировать ещё» — только замена набора (частично спасает избранное).

**Рекомендуемый порядок остатка:** UX-5 (дёшево) → UX-4 (важнее всего для антидубля) → UX-6 → UX-7 → UX-8.

## Проблема

Продавец на Авито (лучший в регионе, 60+ объявлений подписок + 50-150 игр). Для конверсии нужно:
- Несколько объявлений одной подписки/игры в разных регионах
- Уникальные названия и описания для каждого (чтобы не банили за дубли и для A/B)
- Разные обложки (коллажи для подписок, вариации для игр)

API Авито не даёт ключей, платная автоматизация неэффективна → нужен инструмент для **подготовки контента**, который потом копипастом вставляется на Авито.

## Архитектура

- **Формат:** единый HTML-файл (как артефакт), открывается локально в браузере
- **AI:** Claude API через прокси `https://api.poigraem.shop/api/claude` (POST, принимает стандартный Anthropic формат)
- **Хранение:** localStorage для базы объявлений, истории генераций, настроек
- **Стиль:** единый с проектом «Поиграем» — Literata + JetBrains Mono, accent: #B45309, teal: #0F766E

## Модули (в порядке приоритета)

### 1. Генератор описаний и названий (MVP)

**Что делает:** по выбранной подписке/игре генерирует 3-5 уникальных вариантов названия + описания для Авито.

**Интерфейс:**
- Выбор категории: игровая подписка / AI-подписка / цифровой товар / игра
- Для игровой подписки: тип, платформа, срок, цена, список ключевых игр
- Для AI-подписки: сервис, срок, цена, ключевые фичи
- Для цифрового товара: название, описание, цена
- Для игры: название, платформа, цена, жанр
- Выбор стиля: продающий / информативный / краткий / развёрнутый
- Кнопка «Сгенерировать» → 3-5 вариантов названий + описаний
- Кнопка копирования одним кликом (отдельно название, отдельно описание)
- Возможность «перегенерировать» конкретный вариант
- Сохранение удачных вариантов в избранное

**Промпт для Claude (системный):**

```
Ты — эксперт по созданию объявлений на Авито для продажи цифровых товаров: игровых подписок (PS Plus, EA Play, Game Pass), AI-подписок (Claude Pro, ChatGPT, Google AI Pro, Cursor, Antigravity) и цифровых игр.

ЗАДАЧА: генерировать УНИКАЛЬНЫЕ варианты объявлений для одного и того же товара. Каждый вариант должен отличаться не рерайтом, а СТРУКТУРНО — как будто его писал другой продавец.

ПРАВИЛА НАЗВАНИЙ (макс. 50 символов с пробелами):
- Обязательно: название товара + крючок (фишка, выгода, топ-игра, цена за месяц)
- Варьировать: какую игру/фичу вынести в название, как подать цену, какие слова использовать
- Можно объединять подписки: "PS+Deluxe|Extra" или "Все подписки PS+" — это отдельный тип объявления
- Примеры стилей названий:
  * С топ-игрой: "Подписка PS+Extra(Человек Паук 2,GTA5)пс5"
  * С ценой за месяц: "PS Plus Extra 12мес — от 608₽/мес пс4 пс5"
  * С эмоцией: "PS+Deluxe 405+игр — прокачай свою консоль"
  * С фичей: "Claude AI Pro — самая умная нейросеть"
  * Объединённое: "PS+Deluxe|Extra(Хогвартс,гта5)пс4,пс5"

ПРАВИЛА ОПИСАНИЙ:
- Использовать эмодзи (⚡️✅💲🎮🚀🧠🔥), но в разных комбинациях
- Список ТОП-игр/фич — обязательный скелет, но порядок и набор варьировать
- Цены подавать по-разному:
  * Просто цена: "3000 руб за 12 мес"
  * Цена за месяц: "всего 250₽/мес"
  * Сравнение: "дешевле чашки кофе"
  * Экономия: "экономия 40% от помесячной"
- Структуру описания менять:
  * Начать с боли → решение → фичи → цены
  * Начать с цены → что получишь → фичи
  * Начать с списка игр → преимущества → цены
  * Начать с эмоции/метафоры → содержимое → цены
- Стили текста: дерзкий/молодёжный, деловой/информативный, восторженный, минималистичный
- Для AI-подписок: описывать конкретные инструменты и юзкейсы, не абстрактно

ВАРИАТОРЫ (что менять между объявлениями):
1. Название: другая игра/фича в крючке
2. Структура описания: другой порядок блоков
3. Подача цены: помесячная / общая / со скидкой / сравнение
4. Набор игр в списке: ротация из полного каталога
5. Эмодзи: другой набор
6. Тон: от дерзкого до делового
7. Объединение: только Extra / только Deluxe / обе вместе / все PS+
8. Регион: влияет на цену доставки/активации если есть

ФОРМАТ ОТВЕТА (JSON):
{
  "variants": [
    {
      "id": 1,
      "title": "название до 50 символов",
      "title_length": число,
      "description": "полное описание",
      "style": "дерзкий|деловой|восторженный|минимальный",
      "price_format": "помесячная|общая|сравнение|экономия",
      "hook": "что вынесено в крючок названия",
      "structure": "боль→решение|цена→фичи|список→преимущества|эмоция→содержимое"
    }
  ]
}
```

**Входные данные для промпта (передаются в user-сообщении):**
- Категория товара и конкретный продукт
- Платформа(ы)
- Доступные сроки и цены
- Список игр/фич (полный, из которого AI выбирает)
- Сколько вариантов нужно
- Какие уже существующие названия исключить (из базы)
- Дополнительные указания (объединить с другой подпиской, сделать акцент на...)

**Ключевая идея:**
Это не просто генератор текстов. Это КОМБИНАТОРНЫЙ ВАРИАТОР — он должен понимать, что каждое объявление собирается из осей вариации (название × структура × подача цены × набор игр × тон × эмодзи × объединение), и выдавать варианты из РАЗНЫХ точек этого пространства, а не соседних.

**Важно:**
- Названия СТРОГО до 50 символов — проверять длину
- Описания — реально разные по структуре, не рерайт одного и того же
- Учитывать ограничения Авито: длина названия, запрещённые символы, правила площадки
- Для подписок — особенно сложно, т.к. одну подписку нужно описать 5-10 разными способами
- AI-подписки — отдельная аудитория (разработчики, дизайнеры, студенты), другой язык

### 2. Конструктор обложек-коллажей

**Что делает:** собирает обложки из картинок игр в готовый коллаж для Авито.

**Интерфейс:**
- Загрузка обложек игр (drag & drop или выбор файлов)
- Выбор раскладки: сетка 2×2, 3×2, 3×3, полоски, диагональ, свободная
- Наложение текста: название подписки, цена, бейджи ("ЛУЧШАЯ ЦЕНА", "100+ ИГР")
- Настройка фона, рамок, теней
- Превью и скачивание в нужном разрешении (Авито: 1:1 или 4:3)
- Рандомизация: кнопка «другой вариант» меняет раскладку/фон/порядок

**Для игр:**
- Взять одну обложку и сделать вариации: разные рамки, фон, стикер с ценой, платформа-бейдж
- Быстрая генерация 3-5 вариантов одним кликом

**Технически:** Canvas API, всё клиентское, без бэкенда.

## Структура файла

```
avito-helper.html — единый файл
├── <style> — все стили
├── <div id="app"> — точка монтирования
├── <script> — всё приложение
│   ├── state / store (localStorage)
│   ├── router (табы: Генератор / Обложки)
│   ├── module: description-generator
│   ├── module: cover-builder
│   └── utils (copy, download, api-call)
```

## AI-провайдеры и модели

Приложение поддерживает переключение между провайдерами и моделями. **Все запросы идут через прокси на VPS** — прямые вызовы из России блокируются (IP + DPI), прокси обходит это (как уже сделано для основного Поиграем).

### Поддерживаемые провайдеры

| Провайдер | Модели | Эндпоинт прокси | Статус |
|-----------|--------|-----------------|--------|
| Anthropic (Claude) | Sonnet (по умолчанию), Haiku | `/api/claude` | ✅ работает |
| OpenRouter | любые (зоопарк моделей) | `/api/openrouter` | ✅ работает |

**Приоритет по умолчанию:** Claude Sonnet. Haiku — для дешёвых черновых генераций. OpenRouter — для экспериментов с другими моделями.

Ключи хранятся в `.env` на VPS (не в браузере): `ANTHROPIC_KEY`, `OPENROUTER_KEY`.

### Абстракция вызова (клиент)

```javascript
// Настройки провайдера хранятся в localStorage
const AI = {
  provider: 'claude',      // 'claude' | 'openrouter'
  model: 'claude-sonnet-4-20250514',
};

async function callAI(messages, system, opts = {}) {
  const provider = opts.provider || AI.provider;
  const model = opts.model || AI.model;

  if (provider === 'claude') {
    const res = await fetch('https://api.poigraem.shop/api/claude', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        model,                    // claude-sonnet-4-... | claude-haiku-...
        max_tokens: opts.max_tokens || 4000,
        system,
        messages,                 // Anthropic-формат
      })
    });
    const data = await res.json();
    return data.content?.map(b => b.text).join('\n') || '';
  }

  if (provider === 'openrouter') {
    // OpenRouter = OpenAI-формат: system идёт первым сообщением
    const orMessages = system
      ? [{ role: 'system', content: system }, ...messages]
      : messages;
    const res = await fetch('https://api.poigraem.shop/api/openrouter', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        model,                    // напр. 'anthropic/claude-3.5-sonnet', 'google/gemini-...', 'meta-llama/...'
        max_tokens: opts.max_tokens || 4000,
        messages: orMessages,
      })
    });
    const data = await res.json();
    return data.choices?.[0]?.message?.content || '';
  }
}
```

### UI переключения

- Селектор провайдера: Claude / OpenRouter
- Селектор модели (зависит от провайдера):
  - Claude: Sonnet (default), Haiku
  - OpenRouter: текстовое поле или список (можно вписать любую модель вручную)
- Настройка сохраняется в localStorage
- Можно задавать модель на уровне отдельной задачи (черновики — Haiku, финал — Sonnet)

**Важно:** форматы ответов разные — Claude отдаёт `content[].text`, OpenRouter `choices[0].message.content`. Абстракция `callAI()` это скрывает.

## Соглашения по коду

- Vanilla JS, никаких фреймворков (один HTML-файл)
- CSS-переменные для темы
- Модульная структура внутри одного файла (IIFE или объекты-модули)
- Все тексты интерфейса на русском
- Мобильная адаптация (часто работа с телефона)
- Комментарии в коде на русском

## Дизайн

- Тёмная тема (основной режим работы — вечер)
- Шрифты: Literata (текст), JetBrains Mono (код/данные)
- Цвета: фон #1a1a2e, карточки #16213e, accent #B45309 (оранжевый), teal #0F766E
- Скруглённые карточки, мягкие тени
- Минимум декора, максимум функциональности
- Кнопки копирования — заметные, с визуальным фидбеком (✓ скопировано)

## Каталог товаров

### Игровые подписки
- **PS Plus Essential** — PS4, PS5. Сроки: 1/3/12 мес
- **PS Plus Extra** — PS4, PS5. Сроки: 1/3/12 мес
- **PS Plus Deluxe** — PS4, PS5. Сроки: 1/3/12 мес
- **EA Play** — PS4, PS5, Xbox, PC. Сроки: 1/12 мес
- **Game Pass** — Xbox, PC. Сроки: 1/3/12 мес

### AI-подписки и цифровые сервисы
- **Claude Pro** (Anthropic)
- **ChatGPT Plus/Pro** (OpenAI)
- **Google AI Pro** (Google)
- **Cursor** (IDE с AI)
- **Antigravity**
- (и другие — список расширяется)

### Игры
- Цифровые игры для PS4, PS5, Xbox
- 50-150 наименований в ассортименте

### Особенности размещения
- Одна подписка/игра → несколько объявлений в разных регионах
- Каждое объявление должно иметь уникальное название + описание (иначе Авито банит за дубли)
- Обложки тоже желательно разные для разных объявлений
- Подписки AI — особая категория, т.к. аудитория другая и описания нужны технические

## Правила и ограничения Авито

### Лимиты
- **Название:** строго до 50 символов с пробелами
- **Описание:** лимит очень большой — для наших объявлений не актуален
- **Фото:** до 10 штук на объявление; предпочтительно вертикальные

### Запрещено (снимают объявление)
- Ссылки на внешние ресурсы, QR-коды
- Упоминание других каналов связи («пишите в Telegram», «WhatsApp» и т.п.)
- Любые намёки на продажу аккаунта — писать нельзя ни в каком виде

### Обязательные фразы
- **Для игр:** «оформляется в цифре» — обязательно
- **Для игр и подписок:** указывать платформы (PS4/PS5, Xbox и т.п.) — и в названии, и в описании

### CTA (разрешённые)
- Отсылка в личку Авито — не банят: «Не нашли нужную игру? Пишите в личку — у нас большой каталог»
- Мягкий апсейл в конце объявления: упоминание что есть другие игры и подписки

### Как Авито определяет дубли
По фото, по названию и по тексту описания. Одинаковые фразы (особенно длинный список игр) — прямой путь к бану. Поэтому:
- Разные объявления = разные точки в пространстве вариаций
- В одном регионе не размещать слишком похожие объявления

### Оси рандомизации (примеры реальных замен)
**Названия игр** — одно и то же можно написать по-разному:
- `Red Dead Redemption 2` / `RDR 2` / `рдр2` / `rdr2`
- `FC 26` / `FC26` / `FIFA 26` / `ФИФА 26` / `фифа26`
- `UFC 5` / `ЮФС 5` / `юфс5`

**Названия подписок** — варьировать в названии И в теле:
- `PS+ Extra` / `ПС+ Экстра` / `PS Plus Extra` / `ПС Плюс Экстра` / `подписка пс плюс`

**Платформы** — тоже разные форматы:
- `PS4/PS5` / `ПС4,ПС5` / `пс4 пс5` / `для пс4 пс5` / `подписка на пс4,пс5`

**Список игр** — базовый скелет + ротация:
- Не менять весь список, но менять порядок, набор «дополнительных» игр, стиль оформления (запятые / с новой строки / через dash)

### SEO: по каким запросам ищут
Люди ищут игры разными способами:
- На русском: «человек паук», «гта 5», «ведьмак», «призрак цусимы»
- Транслит: «spiderman пс5», «gta5 пс4», «witcher 3»
- Аббревиатуры: «rdr2», «rdr 2», «гта», «мк11»
- С платформой: «игры пс5», «подписка пс плюс», «game pass xbox»
- С ценой: «купить ps plus дёшево», «ps plus 12 месяцев»

Генератор должен учитывать эти варианты при формировании описания.

## Примеры реальных объявлений (эталон стиля)

### Claude Pro (AI-подписка)
**Название:** `Claude AI Pro — самая умная нейросеть` (38 символов)
**Описание:**
```
⚡️ Claude 3.5: Лучший кодинг и живое общение
Подписка PRO на месяц 2000 руб
Забудь про шаблонные ответы. Claude — это самый мощный интеллект для тех, кто ценит качество кода и естественность языка.
Главные инструменты:
* Claude Code: ИИ-агент прямо в твоем терминале. Сам пишет, правит и тестирует код.
* Интеграция: Умная работа внутри Excel и Chrome для мгновенного анализа данных.
* Artifacts: Визуализация кода и интерфейсов в реальном времени прямо в чате.
* Projects: Создавай отдельные базы знаний для каждого своего проекта.
Доступные планы:
* Pro — стандарт индустрии для профи.
* Max — для максимальных нагрузок и лимитов.
```
**Стиль:** технический, уверенный, с конкретными фичами

### EA Play (игровая подписка)
**Название:** `Подписка на FC25, NHL,UFC и др игры PS4 PS5` (49 символов)
**Описание:**
```
⚽Подписка на 12 месяцев позволит скачать на вашу консоль ПС4 или ПС5 такие игры как:
ФИФА 25 (FC25), UFC 5, It Takes Two, ЮФС4, Star Wars Jedi: Survivor, НХЛ 24, NHL 26,
NFS 2015, Need For Speed Heat и др игры NFS, UFC3 Battlefield 2042, Battlefield 1,
Battlefield 5, Mass effect Legendary и множество других (список на фото).
Вы сможете абсолютно свободно распоряжаться играми в течение всего срока подписки.
💲1 месяц - 850
💲12 месяцев - 3000
```
**Стиль:** информативный, список игр как главный аргумент, простые цены

### PS Plus Extra/Deluxe (игровая подписка, объединённое объявление)
**Название:** `Подписки пс+Экстра/Делюкс(Хогвартс,гта5)пс4,пс5` (50 символов)
**Описание:**
```
Подписки PS+ Extra и PS+ Deluxe для ПС4 и ПС5 прокачают вашу консоль,
словно закись азота тачку Доминика Торетто: вы мгновенно получите 405+ игр:
Warhammer 40 000 Space Marine 2, Человек Паук 2, Одни из Нас 2, GTA 5,
God Of War Ragnarok, Silent Hill 2, Cyberpunk 2077, Хогвартс, Скайрим,
Принц Персии, Doom, Призрак Цусимы, MK11, Lego City, NFS Unbound,
Far Cry 6, Demon's Souls, Assassins Creed Valhalla и многих других.
⚡️ Преимущества подписки:
✔️ Возможность играть по сети
✔️ Новые игры добавляются каждый месяц — скучать не придется!
✔️ Возможны различные сроки подключения:
✅3 месяца - 3000 рублей (всего от 1000 руб в месяц)
✅1 месяц - 1400 рублей
✅12 мес от 7300
```
**Стиль:** дерзкий/молодёжный, метафора, длинный список игр, цены в конце

### Google AI Pro (AI-подписка)
**Название:** `Google Ai Pro 6 месяцев(Gemini 3.1 PRO)топовый ии` (50 символов)
**Описание:**
```
🚀 Google AI Pro — Твой интеллектуальный апгрейд
Забудь про рутину. Получи доступ к технологиям, которые делают 90% работы за тебя:
от анализа терабайтов данных до создания визуальных шедевров и даже музыки.
🧠 Gemini 3.1 Pro — ультимативный мозг для кода, стратегий и текстов.
🎬 Veo & Nano Banana — твоя личная киностудия и топовый фотограф.
📓 NotebookLM — превращает гору файлов в четкие инсайты.
🔍 Deep Research — агент, который находит в сети всё за тебя.
🧪 Google Labs — эксклюзивные фишки раньше всех в мире.
☁️ 2 000 ГБ памяти — безлимитный комфорт для всей семьи.
💰 1800 ₽ на 6 месяцев (300 ₽/мес — дешевле чашки кофе)
```
**Стиль:** восторженный, каждая фича = эмодзи + однострочник, цена-сравнение в конце
**Обложка:** Мега-мозг с логотипом Gemini — пример креативного подхода к обложкам AI-подписок

## Оси вариации объявлений

Каждое объявление — точка в многомерном пространстве. Чем больше осей отличаются, тем более "другим" выглядит объявление:

| Ось | Варианты |
|-----|----------|
| Название: крючок | топ-игра, цена/мес, эмоция, фича, количество игр |
| Название: объединение | только один тип / два типа вместе / все вместе |
| Название подписки | PS+ Extra / ПС+ Экстра / PS Plus Extra / ПС Плюс / подписка пс плюс |
| Платформы | PS4/PS5 / ПС4,ПС5 / пс4 пс5 / для пс4 пс5 / подписка на пс4,пс5 |
| Названия игр | оригинал / транслит / аббревиатура / русское название |
| Описание: структура | боль→решение / цена→фичи / список→плюсы / метафора→контент |
| Описание: подача цены | общая / помесячная / сравнение / экономия % |
| Описание: тон | дерзкий / деловой / восторженный / минимальный |
| Описание: набор игр/фич | ротация из полного каталога (скелет + разные дополнения) |
| Описание: оформление списка | запятые в строку / с новой строки / через тире |
| Эмодзи | разные наборы, разная плотность |
| Регион | разные города (в одном регионе не размещать слишком похожие) |
| Обложка | разные коллажи / раскладки / фоны |

## Зависимости от основного проекта

- Прокси `/api/claude` на VPS (работает)
- Прокси `/api/openrouter` на VPS (работает)
- Ключи `ANTHROPIC_KEY`, `OPENROUTER_KEY` в `.env` на VPS
- Supabase — пока не используется (возможно позже для синхронизации базы между устройствами)

## TODO

- [ ] Актуализировать цены во всех примерах
- [ ] Добавить полный каталог игр для каждой подписки (для ротации в описаниях)

---

## Команда: /session-end

Когда Денис пишет `/session-end`:
1. Обнови раздел `## Текущий фокус` вверху файла актуальным состоянием
2. Добавь НОВУЮ запись В НАЧАЛО раздела `## Лог сессий` (не редактируй существующие!):
   - **Дата** (текущая)
   - **Что сделано** — конкретные изменения в коде/фичах
   - **Что решили** — архитектурные/продуктовые решения этой сессии
   - **Новые баги** (если нашли) — с указанием файла и описанием
   - **Открытые вопросы** — что обсуждали но не решили
   - **Что осталось** — задачи на следующую сессию
3. Если в логе стало больше 5 записей — самые старые (сверх 5) перенеси в `SESSIONS-ARCHIVE.md` (в начало файла, создай его если нет). В CLAUDE.md всегда держим 5 последних сессий.
4. **Перенос закрытого в `AUDITS-ARCHIVE.md`** (создай файл если нет): пройдись по CLAUDE.md и найди аудиты/находки/задачи, которые за эту сессию стали ПОЛНОСТЬЮ закрытыми (целый аудит, или отдельные закрытые находки внутри ещё открытого аудита, или закрытый пункт бэклога). Перенеси текст в `AUDITS-ARCHIVE.md` с пометкой «✅ закрыт, сессия N, коммит(ы) …», в CLAUDE.md оставь строку-указатель и поправь шапку аудита, если она устарела. ⚠️ Не переноси, если закрытие не подтверждено явно (код готов, но не проверено — это НЕ закрытие).
5. **Проверка незакоммиченных/незапушенных правок:** выполни `git status --short` и проверку незапушенных коммитов (`git log --branches --not --remotes --oneline`), покажи Денису список, напомни закоммитить/запушить. Не коммить сам без явной просьбы.
6. Спроси: «Что ещё зафиксировать?»

## Команда: /session-start

Когда Денис пишет `/session-start` или начинает новый чат с упоминанием проекта:
1. Прочитай раздел `## Текущий фокус`
2. Прочитай 2-3 последние записи в `## Лог сессий`
3. Резюмируй: «В прошлый раз остановились на [...]. Открыто [...]. Следующий шаг был [...].»
4. Спроси: «Продолжаем с этого или новое направление?»

## Команда: /session-archive

Когда Денис пишет `/session-archive`:
1. Прочитай `SESSIONS-ARCHIVE.md`
2. Резюмируй ключевые моменты — решения, баги, паттерны
3. Спроси: «Что именно ищем?»

## Команда: /session-audits-archive

Когда Денис пишет `/session-audits-archive`:
1. Прочитай файл `AUDITS-ARCHIVE.md` (закрытые аудиты, задачи и находки, вынесенные из CLAUDE.md)
2. Резюмируй ключевые моменты — какие аудиты/находки закрыты, что было исправлено, важные паттерны
3. Спроси: «Что именно ищем?»

---

## Лог сессий

### 2026-07-11 — Переработка «рандомных заголовков» (аудит Fable 5) + хардинг фиксации выбранного названия

**Контекст:** Денис попросил вывести незакрытые пункты последних аудитов Fable, выбрал порядок работ и указал начать с пункта 2 — переработки заголовков (главная его боль: «промт выносит рандомные слова»). Через AskUserQuestion выбрал делать ВСЁ вместе, включая смену воронки (↻×5 первым шагом), а не только промпт-правки. Исправления принимал большим пакетом. На следующий день (07-12) прогнал ревью кода сессии и попросил захардить найденный зазор.

**Что сделано (`avito-helper.html`, коммиты `595622a` + `8289461`, запушены):**
- **Формула названий в SYS** (`595622a`) — блок «ПРАВИЛА НАЗВАНИЙ» переписан под формулу «ПОИСКОВОЕ ЯДРО + ПЛАТФОРМА/СРОК + ОДИН КРЮЧОК», явный запрет наваливать несколько крючков, требование пробелов вокруг скобок; эталонные примеры почищены (`PS+ Extra (GTA 5) пс4/пс5` вместо слипшегося `PS+Extra(GTA5)пс5`).
- **Ядро/маскировка раздельно** (`595622a`) — `buildGamesList`→`buildGamesLists` возвращает `{core, masking}` отдельными полями; новый `catInfoBlock` кладёт ядровые в секцию «Ключевые игры (крючок ТОЛЬКО отсюда)», маскирующие — в «Доп. игры для ОПИСАНИЯ (в крючок НЕ брать)». Ручной ввод продавца (`f.games`) считается ядром. Закрывает корень «рандомных слов» — раньше модель могла утянуть в заголовок случайную маскирующую игру.
- **Few-shot из избранного** (`595622a`) — `fewShotBlock(category)` подмешивает 1-2 сохранённых удачных объявления той же категории как образец характера продавца (обрезка описания до 220 симв., «не копируй дословно»). Зависит от `_category`-штампа (F4) — на избранном до F4 молча пусто.
- **`temperature: 0.8`** (`595622a`) — `callAI` задаёт 0.8 по умолчанию (было 1.0 — для 50-символьной строки давало кашу вместо крючка), переопределяется через `opts.temperature`. Проброшено в оба провайдера (Claude + OpenRouter).
- **Воронка «сначала заголовки»** (`595622a`) — новый первый шаг: `doGenTitlesOnly` генерит 8-12 названий (`count*2`, клампы 8..12) → промежуточный экран `titleStepHTML` с чекбоксами → `doGenDescriptionsForTitles` пишет описания ТОЛЬКО для выбранных. Кнопка формы «🏷️ Сначала заголовки (шаг 1 из 2)» + вторичная «⚡ Сразу полный вариант» (старый путь `doGenerate` сохранён). `state.titleStep`/`loadingKind` добавлены; batch-режим взаимоисключён с шагом заголовков (guard в `toggle-batch`). Обработчики `gen-titles`/`title-pick`/`titles-next`/`titles-regen`/`titles-cancel`.
- **Хардинг фиксации названия** (`8289461`) — `doGenDescriptionsForTitles` теперь прописывает `title`/`title_length` из ВЫБРАННЫХ названий поверх ответа AI (маппинг по индексу): раньше `parseVariants` брал что вернула модель, и при дрейфе (эмодзи/пробелы/правка) в результат уходил изменённый заголовок — вся суть шага в том, что продавец выбрал ТОЧНЫЕ названия. Если AI вернул меньше описаний, чем выбрано → обрезка до пришедших + тост; пустой ответ → понятная ошибка.

**Проверка:** `node --check` на извлечённом inline-скрипте + vm-песочница со стабами `fetch`/DOM/localStorage — 9 тестов: разделение ядро/маскировка (маскирующие никогда в hook-секции), фильтр few-shot по категории, `temperature` дефолт+override, `doGenTitlesOnly` наполняет titleStep, no-op без выбора, точное сохранение названия на честном ответе, **принудительный возврат названия при дрейфе AI** (эмодзи/`!!!`/пробелы → выбранное на месте, описание цело), **обрезка при недоборе описаний** (1 из 3 → без краша/ошибки). Все прошли.

**Что решили:**
- Делать переработку заголовков целиком (промпт + temperature + few-shot + смена воронки), а не только промпт-правки — выбор Дениса через AskUserQuestion.
- Хардить фиксацию выбранного названия по индексу (не оставлять на промпте) — по итогам ревью; при недоборе описаний обрезаем, а не падаем.
- Старый путь «сразу полный вариант» оставлен как быстрая альтернатива воронке.
- Всё реализовано в КОДЕ, но по стандарту проекта НЕ закрыто (нет визуальной приёмки) — в `AUDITS-ARCHIVE.md` не переносится, аудит Fable 5 остаётся открытым до проверки глазами.

**Инцидент с параллельной сессией:** во время работы параллельная сессия Claude Code делала `git rebase`/`reset` в этом же репо (её коммиты content-creator/price-lookup видны в reflog) — это ДВАЖДЫ откатывало мои правки `avito-helper.html` с диска, хотя они числились «внесёнными». Пойман через несоответствие в sandbox-тестах, восстановлен по частям с перепроверкой после каждой. Также обнаружено: запись лога «сессия 2 — F2/F5/F6/F7/F8» пропала с диска (код `0a31030` закоммичен, а CLAUDE.md откатился) — НЕ восстановлена в этой сессии, вынесена в открытый вопрос. Все коммиты делались явным путём `git add -- Standalone/avito-helper/avito-helper.html`, чужие изменённые `CLAUDE.md`/`SESSIONS-ARCHIVE.md` в корне не тронуты.

**Новые баги:** не найдено (реализация из готового плана + ревью + хардинг).

**Открытые вопросы:**
- Визуальная приёмка ВСЕГО аудита Fable 5 (F1-F8 + заголовки) на живом инструменте — не проведена.
- Восстанавливать ли потерянную запись лога «сессия 2 F2-F8» (текст есть в истории диалога) — ждёт решения Дениса.
- Риск повторного отката, пока в репо активна параллельная сессия.

**Что осталось:**
1. Визуальная приёмка заголовков: генерация → шаг «Сначала заголовки» → выбор → описания с точными названиями; заголовки чище прежних. Плюс приёмка F1-F8 (детали — в записи 2026-07-10 и потерянной «сессия 2»).
2. Решение по восстановлению записи «сессия 2 F2-F8».
3. Прежние нерешённые: этапы 3–4 GFX-аудита (GFX-8..14), UX-9..15, баги #5–#8, каталоги реальными играми, цены на подписки.

### 2026-07-10 — Диета CLAUDE.md + фиксы F1/F3/F4 из аудита Fable 5

**Контекст:** Денис попросил прогнать чистку `CLAUDE.md` по общему гайду `../../CLAUDE-MD-DIET-GUIDE.md` (тот же приём, что в корневом poigraem), затем выбрал чинить красные баги аудита Fable 5 — по одному: F1, затем F3+F4.

**Что сделано:**
- **Диета `CLAUDE.md`** (коммит `623ef22`): лог сессий сокращён с 10 до 5 последних — 5 старейших (2026-06-26 ×2, 2026-06-25 ×2, 2026-06-12) перенесены дословно в начало `SESSIONS-ARCHIVE.md`. Создан `AUDITS-ARCHIVE.md`: вынесены закрытые находки внутри ещё открытых аудитов — GFX-1..7 + GFX-15 (этапы 1+2, коммиты `8ef6039`+`a5d7e75`) и UX-1..8 (`b87b877`+`af14b01`); в `CLAUDE.md` остались открытые GFX-8..14 и UX-9..15 с указателями на архив, шапки аудитов поправлены. Команда `/session-end`: порог хранения 10→5, добавлены шаги переноса закрытого в `AUDITS-ARCHIVE.md` и проверки git-статуса. Добавлена команда `/session-audits-archive`. Сжат раздутый «Следующий шаг» (дубль лога 08.07), актуализирован путь git (`app-3.5.12`→`app-3.6.1`). Размер: 105.9→~82 КБ (−22%). Чужие незакоммиченные файлы (корень, content-creator, price-lookup) не тронуты.
- **F1** (в составе коммита `b1756d0`): убран `onclick="event.stopPropagation()"` с `.batch-row-btns` (`refreshBatchResults` ~1417). Приложение слушает клики одним делегированным `document`-listener (4163) через `e.target.closest('[data-act]')` (4305) → `stopPropagation` на обёртке глушил все 5 кнопок свёрнутой строки пакета. Двойного разворота нет: `closest()` выбирает саму кнопку, а не `batch-expand` головы строки.
- **F3** (тот же коммит): новый хелпер `extractJSON(text)` — сначала ```json-блок```, иначе вырез от первого `{` до последнего `}`. Подключён в `parseVariants` и в `doGenAltTitles` (убран дублирующий хрупкий инлайн-парс). Проза от веб-поиска бонусов предзаказа больше не роняет `JSON.parse`. Проверено node-тестом на 3 кейсах (проза вокруг / fenced / сырой JSON).
- **F4** (тот же коммит): при генерации каждый вариант штампуется `_product`/`_category` по форме на момент генерации, форма снимается в `state.resultsForm` (в `doGenerate` и `doBatchGenerate`). Читатели переведены на штамп с фолбэком на текущую форму: `mark-posted`, `batch-mark-posted` (товар), `scan-check` (категория), `exportPackage` (товар), `doGenAltTitles`/↻×5 (`buildMsg(1, [], state.resultsForm)`). Избранное (`{...variant}`, 1142) наследует штамп автоматически.
- `node --check` на всём инлайн-скрипте — OK. Оба коммита запушены (`4404512..623ef22 main`).

**Что решили:**
- Диету и код-фиксы залить двумя раздельными коммитами (док-диета vs код), строго в рамках avito-helper — чужие параллельные правки в репо не трогать.
- F3 расширен на `doGenAltTitles` (та же поломка парса), но `max_tokens:600` там НЕ трогали — это отдельный F5, не в этом заходе.
- F1/F3/F4 задеплоены, но по стандарту проекта НЕ закрыты (нет визуальной приёмки Денисом) — в архив не переносятся, остаются открытыми до проверки на живом сайте.

**Новые баги:** не найдено (реализация из готового аудита + чистка).

**Открытые вопросы:** порядок остатка аудита 08.07 — F5 (↻×5 max_tokens) vs переработка «рандомных заголовков» — не выбран.

**Что осталось:**
1. Визуальная приёмка F1 (5 кнопок свёрнутой строки пакета), F3 (генерация предзаказа не пустая), F4 (сгенерировать игру → переключить форму на подписку → Размещено/Сканер/Экспорт показывают игру) на живом сайте после деплоя
2. F2 (пакет от ~7 регионов — `max_tokens`), F5 (↻×5 `max_tokens:600`), F6–F8 мелочи
3. Переработка заголовков (промпт-формула + few-shot + `temperature:0.8` + ↻×5 первым шагом)
4. Прежние нерешённые: этапы 3–4 GFX-аудита (GFX-8..14), UX-9..15, баги #5–#8, каталоги реальными играми, цены на подписки

### 2026-07-08 — Аудит кода Fable 5 (заголовки/логика/картинки) + идеи прокачки — правок НЕ было

**Контекст:** Денис попросил свежий аудит самой мощной моделью (Fable 5) — сплошное чтение `avito-helper.html` (~4890 строк) на предмет неработающих функций, нарушений логики, и отдельно — идеи развития инструмента. Особый акцент: качество заголовков («промт туда выносит какие-то рандомные слова»), текстов, инструментов картинок.

**Что сделано:** только чтение и анализ, ни одной правки в файле. Прочитан весь файл целиком (константы, промпт, генератор, пакетный режим, очередь/сессия постинга, сканер, детектор дублей, история, весь модуль обложек `cv*`, делегированные обработчики). Прежние фиксы (UX-1..8, GFX-1..7/15, баги #9–19) подтверждены на месте; известные нерешённые (#5–#8, GFX-8..14, UX-9..15) не пересказывались — фокус на новом.

**Найдено (новые баги, ещё НЕ исправлены):**
- 🔴 **F1** — все 5 кнопок в свёрнутой строке пакета (📋 Назв/Опис, 🖼️, 🔍, ✓) мертвы: `onclick="event.stopPropagation()"` на обёртке (`refreshBatchResults`, ~1417) режет всплытие раньше единого делегированного слушателя на `document` — приложение построено на event delegation, локальный `stopPropagation` его ломает. Кнопки в развёрнутом теле строки (без этого атрибута) работают.
- 🔴 **F2** — пакет от ~7 регионов молча падает: `doBatchGenerate` просит все варианты одним запросом при фиксированном `max_tokens:4000` (`callAI`, 853) — ответ обрезается посреди JSON, `parseVariants` падает, результат — пустой пакет без объяснения.
- 🔴 **F3** — предзаказ (`webSearch`) иногда ломает парсинг: модель пишет пояснительный текст до/после JSON, `callAI` склеивает все текстовые блоки, `parseVariants` не умеет вырезать JSON из окружающего текста.
- 🟠 **F4** — «✓ Размещено», «🔍 Проверить», Экспорт и ↻×5 берут товар/категорию из ТЕКУЩЕЙ формы генератора, а не из самого варианта/избранного — если форма была переключена после сохранения варианта, история дублей и подсказки «уже размещалось» получают неверный товар.
- 🟠 **F5** — `doGenAltTitles` (↻×5) передаёт `webSearch` при `max_tokens:600` — веб-поиск съедает бюджет ответа, 5 названий обрезаются.
- 🟡 **F6** — мёртвые ветки `cv-sc-custom`/`cv-stc-custom` в click-делегате (селектор `cvEl` их не матчит, обработка дублируется в input-делегате).
- 🟡 **F7** — пороги подсветки длины названия разъехались между карточкой (жёлтый с 45) и Сканером (с 41).
- 🟡 **F8** — замена обложки игры не освобождает `URL.revokeObjectURL` старого blob — микро-утечка при долгом подборе.

**Диагноз главной боли (заголовки «рандомные слова»):** промпт учит плохому своими же эталонами (нагромождения в скобках без пробелов), «крючок» ничем не ограничен (может утянуть случайную маскирующую игру из рандомизированного списка), `temperature` не передаётся в `callAI` (дефолт 1.0 — максимум креатива для 50-символьной строки), нет требования держать поисковое ядро («подписка пс плюс», «гта 6 пс5») в начале названия.

**Предложенный план (не начат):** переписать блок правил названий в SYS под формулу «поисковое ядро + платформа/срок + один крючок» с чистыми эталонами; ограничить крючок 2–3 первыми ядровыми играми (не маскирующими); few-shot из собственного избранного продавца в `buildMsg`; `temperature:0.8`; сделать ↻×5 первым шагом воронки (10 заголовков → выбор 3 → только тогда описания), а не запасным; автовалидация каждого варианта через `checkAds` сразу после генерации.

**Идеи прокачки продукта (брейншторм, не начат):** CTR-аналитика заголовков (ручной ввод просмотров/контактов из статистики Авито → AI-сводка «что работает»); кнопка «заполнить из PS Store» для формы игры (название+жанр+обложка одним запросом — обложка уже так подтягивается, остальное нет); массовый конвейер (вставить список из N игр строками → авто-заполнение очереди); бейдж «🕐 старше 30 дней» в Истории + фильтр; светофор-проверка всего пакета разом (сканер+дубли); «🎲 Случайный вариант» для обложек (рандомизация фон/тон/зеркало/позиция стикера одной кнопкой — настоящий антидубль вместо нынешних 4 похожих вариантов); плашка зачёркнутой старой цены; самохостинг шрифтов (решает GFX-13/UX-12 разом, как уже сделали в основном магазине).

**Что решили:** аудит — чисто разведка, без правок. Денис попросил зафиксировать план через `/session-end` без выбора порядка работы — старт не начат.

**Новые баги:** F1–F8 выше — обнаружены, НЕ исправлены.

**Открытые вопросы:** не выбран порядок реализации — тройка тихих поломок генератора (F1+F2+F3, ~30 мин) vs. заголовки (правки промпта + temperature + few-shot, самый заметный для Дениса результат) vs. F4+F5 vs. фичи развития.

**Что осталось:**
1. Решить с Денисом порядок: F1/F2/F3 (тихие поломки) → заголовки (промпт+temperature+few-shot) → F4/F5 → идеи развития — рекомендованный порядок, либо другой
2. Всё остальное из плана выше не начато
3. Прежние нерешённые пункты не сдвинулись: баги #5–#8, этапы 3–4 GFX-аудита (GFX-8..14), UX-9..15, тест багов #9–#19 и всех прошлых фиксов на живом сайте, каталоги реальными играми, актуальные цены на подписки

### 2026-07-03 (сессия 2) — Фиксы 5 оранжевых находок UX-аудита (UX-4,5,6,7,8)

**Что сделано (`avito-helper.html`, коммит `af14b01`):**
- **UX-4 (история размещений):** `session-done` теперь пишет в `state.db` не только товар/регион, а полный `title`/`description` варианта. Вкладка «database» (мёртвый код с сессии, когда кнопку убрали из nav — Bug #7) возвращена в шапку как «🗂 История» — видна и кликабельна. Новые действия `mark-posted` (список/конвейер: кнопка «✓ Размещено», спрашивает регион через `prompt()`, product берётся через `currentProductName`) и `batch-mark-posted` (строка пакета: регион уже известен из `item.region`, без prompt). `dupCheck` добавил третий пул сравнения — `state.db` с непустым title/description, метка `📋 История · регион, дата`. В таблице «История» у автозаполненных записей значок 📝 с тултипом (название+описание) — визуальный индикатор, что запись участвует в дедупликации.
- **UX-5 (пакет без регионов):** кнопка «✨ Сгенерировать»/«📦 Сгенерировать пакет» дисейблится и показывает «⚠ Выберите регионы для пакета», пока `batchMode` включён, а `batchRegions` пуст; тот же гвард продублирован тостом в `doGenerate` (на случай программного вызова в обход кнопки). Partial-update обработчик `batch-region` (раньше трогал DOM напрямую без полного рендера) синхронизирован с той же логикой disabled/текст/цвет подсказки.
- **UX-6 (favId по индексу):** `queueForm.favId` и опции `<select id="qf-fav">` переведены с индекса массива на стабильный `_fid` (как везде в избранном). `doStartSession` резолвит `state.favorites.find(f => f._fid === item.favId)` вместо `state.favorites[item.favId]`. При переключении источника на «★ Из избранного» подставляется `_fid` первого элемента, если текущий невалиден (favorite удалили).
- **UX-7 (сканер оторван от карточек):** новая функция `goToScanner(title, desc, category)` — пишет `state._scanPrefill`, переключает вкладку, `scannerTabHTML()` рендерит `value=`/`selected` из прасша, `bindScannerTab()` сразу зовёт `runCheck()` и очищает прасш. Кнопка «🔍 Проверить» добавлена в `vcardHTML` (список), `conveyorHTML` (конвейер), строки пакета (`data-i` вместо `data-vid`/`data-fid`).
- **UX-8 (🖼️ ничего не переносит):** новая `goToCoverSearch(name)` — переключает `cv.mode` в `'game'` (с тем же `confirm()`, что и ручное переключение, если в коллаже уже есть картинки), кладёт имя в `cv._pendingSearchName`, рендерит. `bindCoversTab()` в начале потребляет `_pendingSearchName`: подставляет в `#cv-igdb-game` и программно кликает `[data-act="cv-igdb-game-fetch"]` — поиск стартует сам. `conv-photo` теперь для категории «Игра» зовёт `goToCoverSearch(f.gameName)`, для остальных категорий — как раньше, просто переключает вкладку (осознанно: game-name поиск не подходит для подписок).

**Что решили:**
- Оживить вкладку «История» (была сознательно скрыта в прошлой сессии как «неиспользуемая ручная база») вместо создания новой сущности — теперь она наполняется автоматически из реального постинга, а не ручным вводом, так что причина её скрытия («не нужна») отпала.
- «✓ Размещено» в обычном потоке спрашивает регион через `prompt()` (не модалку) — минимальный UI, консистентно с уже существующим `prompt()` для переименования пресетов.
- UX-8 сознательно ограничен категорией «Игра» — авто-перенос имени в поиск обложки для подписок/AI/цифровых товаров не имеет смысла (там `gameName` не заполняется, используется коллаж из нескольких обложек, а не поиск одной по имени).
- Тестирование: браузерное расширение Chrome было недоступно (`tabs_context_mcp` возвращал «not connected» несколько раз подряд, пользователь подтвердил решение продолжить без него) — вместо этого логика прогнана через Node `vm`-песочницу: извлечён inline-скрипт, застаблены `document`/`localStorage`/`fetch`/`confirm`/`prompt`, отдельно проверены (а) чистые функции (`dupCheck` с историей, `currentProductName`), (б) HTML-рендер (`formHTML` disabled/hint, `queueTabHTML` selected по `_fid`, `scannerTabHTML` префилл, наличие новых кнопок в `vcardHTML`/`conveyorHTML`/batch-строках), (в) полный делегированный click-обработчик — захвачен реальный listener через стаб `addEventListener`, вызван с фейковым `e.target.closest(...)` для `mark-posted`/`batch-mark-posted`/`scan-check`/`conv-photo`. Все проверки прошли. `node --check` — синтаксис ОК.

**Новые баги:** не обнаружено.

**Открытые вопросы / инцидент с git (важно):** во время пуша обнаружилось, что **другая параллельная сессия Claude Code работает в ТОМ ЖЕ локальном репозитории** (не отдельная копия) — правила `CLAUDE.md`, `SESSIONS-ARCHIVE.md`, `Standalone/avito-helper/CLAUDE.md`, `Standalone/avito-helper/SESSIONS-ARCHIVE.md`, `Standalone/content-creator/content-creator.html` и `js/client.js` живьём, пока я делал `git stash`/rebase/push для своего коммита. Я по ошибке `git stash drop` не тот индекс (сдвинулся из-за гонки) и временно потерял 4 из этих файлов — **восстановлено** через `git fsck --no-reflog` (дроп не удаляет объект сразу, коммит остаётся «dangling») + `git checkout <hash> -- <файлы>`. Итог сверен трижды (`git status`/`git diff --stat`/`git log` локально и на origin) — ничего не потеряно, работа другой сессии (включая её собственные 2 коммита `850e2e5`, `2daa3c9`, которые она запушила сама во время моей возни со стешами) цела. Денису стоит проверить, не потеряется ли работа той сессии, если она не завершится явным `/session-end` — это уже второй раз за две сессии подряд (см. открытый вопрос сессии 59 в главном `CLAUDE.md`).

**Что осталось:**
- 🆕 Визуальная проверка вручную (Денисом): «✓ Размещено» на карточке/конвейере спрашивает регион и появляется в «🗂 История»; «🔎 Дубли» находит совпадение с записью истории; пакет без регионов не даёт нажать «Сгенерировать»; удаление избранного не ломает уже добавленную в очередь позицию «из избранного»; «🔍 Проверить» на карточке открывает Сканер с готовыми данными; 🖼️ на карточке игры сразу ищет обложку
- 🟡 Жёлтые UX-9…UX-15 (полировка)
- Этапы 3–4 GFX-аудита (GFX-8…GFX-14)
- Тест багов #9–#19 и предыдущих UX/GFX-фиксов на живом сайте вручную; каталоги реальными играми; цены на подписки; старые баги #5–#8 (Bug #7 теперь частично неактуален — вкладка снова в UI, но мануальная форма добавления там осталась как была)

### 2026-07-03 — UX-аудит рабочего процесса + фиксы трёх красных (UX-1,2,3)

**Что сделано:**
- **UX-аудит воронки продавца** (форма генератора → варианты список/конвейер/пакет → очередь → сессия постинга → сканер → настройки) сплошным чтением. 15 находок UX-1…UX-15 вписаны в новый раздел «Пользовательский аудит рабочего процесса (UX)». Отдельно от графического GFX-аудита.
- **Три красных исправлены** (`avito-helper.html`, коммит `b87b877`, запушен явным путём):
  - **UX-1** — очередь «авто» генерировала не тот товар. Корень: `doStartSession` слал `buildMsg(autoItems.length)` из текущей `state.form`, поле `product` позиции в AI не уходило → несколько разных авто-товаров генерились из одной формы. Фикс: `buildMsg` принял 3-й аргумент `formOverride`; позиция «авто» при добавлении снимает снимок формы (`item.form`, сохраняется в `avito_queue`); `doStartSession` переписан на цикл — каждая авто-позиция генерируется из своего снимка (1 запрос/позиция, прогресс `N/M`), старые позиции без снимка → из текущей формы; авто-позиция с невалидной формой в очередь не добавляется; продукт-подпись автозаполняется из формы; радиокнопка переименована в «✨ AI из текущей формы генератора».
  - **UX-2** — `doGenerate` не проверял форму, бил в AI на пустых полях. Фикс: новая `validateForm(f)` по категориям + guard в `doGenerate` → тост «Заполните: …», без запроса к AI.
  - **UX-3** — пакет и обычные варианты молча затирали друг друга (`refreshResults` уходит в пакетную ветку при `batchResults.length`, обычные `state.results` оставались скрытыми в localStorage и «воскресали» после «✕ Очистить»). Фикс: `doBatchGenerate` чистит `state.results`/`conveyorIdx` — состояния взаимоисключающие.
- **Проверка:** `node --check` на извлечённом JS (OK). Затем — не только синтаксис: поднят локальный сервер (`node http`, порт 8765), приложение открыто в Chrome через MCP, прогнаны реальные проверки БЕЗ обращения к AI: `validateForm` возвращает верные списки по категориям; `buildMsg` реально использует переданный `formOverride`; клик «Сгенерировать» на пустой форме игры → тост «Заполните: название игры, платформы», сетевого запроса нет (перехват `fetch`), `state.loading` не встаёт; клик «+ в очередь» с source=auto → позиция со снимком (`form.gameName='Elden Ring'`), продукт автозаполнен; невалидная авто-форма отклонена с тостом. Сервер остановлен, тестовый localStorage очищен.

**Что решили:**
- Приписка «авто» = «сгенерировать из ТЕКУЩЕЙ формы» — но зафиксировать её снимком в момент добавления, чтобы очередь из разных товаров работала (каждая позиция помнит свой товар). Цена — по одному запросу на авто-позицию вместо одного общего; очереди небольшие, корректность важнее. Возможная поздняя оптимизация — группировать одинаковые товары.
- UX-аудит нумеруется UX-N (рабочий процесс), отдельно от GFX-N (графика обложек) — это разные срезы.
- Красные закрыты в одном коммите; оранжевые/жёлтые оставлены Денису на выбор порядка.

**Новые баги:** не обнаружено. UX-6 пересекается с известным Bug #5 (favId по индексу).

**Открытые вопросы:**
- Цикл авто-генерации очереди (`doStartSession`) на реальном AI-запросе вживую не гонялся — проверена только проводка по частям (снимок, override, ключевание `autoVariants` по id).
- В рабочей копии репо висят незакоммиченные параллельные правки в файлах магазина (`index.html`, `js/admin-core.js`, `js/client.js`, `js/cloud-sync.js`, `styles.css`) — НЕ мои, не трогал, в коммит не попали (коммитил явным путём только `avito-helper.html`). Та же ситуация «параллельные сессии», что отмечалась раньше.

**Что осталось:**
- 🟠 Оранжевые UX-4 (память о постах), UX-5 (пакет без регионов), UX-6 (favId), UX-7 (сканер↔карточки), UX-8 (🖼️ перенос) — рекомендованный порядок UX-5 → UX-4 → UX-6 → UX-7 → UX-8
- 🟡 Жёлтые UX-9…UX-15
- Этапы 3–4 GFX-аудита (GFX-8…GFX-14)
- Тест багов #9–#19 и UX/GFX-фиксов на живом сайте вручную; каталоги реальными играми; цены на подписки; старые баги #5–#8

<!-- Записи добавляются автоматически командой /session-end -->
<!-- Записи до 2026-07-02 (сессия 2) включительно перенесены в SESSIONS-ARCHIVE.md (держим 5 последних) -->
