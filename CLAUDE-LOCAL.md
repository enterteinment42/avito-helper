# avito-helper — журнал сессий (Windows)

История работы с машины Дениса. Записи добавляются **в начало** раздела «Лог сессий»,
старые не редактируются. Больше пяти — самые старые уезжают в `SESSIONS-ARCHIVE.md`.
**Размер одной записи — не больше 8 КБ.**

⚠️ У проекта есть второй журнал — `CLAUDE-VPS.md` в папке `~/avito-helper` на сервере.
Он равноценен этому. Пиши в журнал той машины, на которой работаешь.

---

## Прежний «Текущий фокус» (перенесён целиком, 22.09.2026)

Раздел в том виде, в каком он жил в `CLAUDE.md` до прополки: 75 КБ нарратива,
накопленного с июля. Живые пункты вынесены в чек-лист указателя, здесь — весь
контекст, включая обоснования решений Дениса.

## Текущий фокус

**Этап:** ✅ **ПЛАН СБЛИЖЕНИЯ С АЙДЕНТИКОЙ ЗАКРЫТ ЦЕЛИКОМ (сессия 2026-09-22).** Денис прогнал фото консолей в интерьере — **работает**, это было последнее непроверенное звено 2б (у всех прежних проб `refs=0`). Направления 1, 2а, 2б и инфографика закрыты; текст плана — в `AUDITS-ARCHIVE.md`.
**Сделано в ту же сессию (два коммита, оба запушены, стенд 1067/1067):** `0c84420` — **ротация объявлений и учёт по аккаунтам Авито** (две степени переписывания, порог 14 дней, «✓ Разместил» заводит новую запись и не трогает сырьё прежней; аккаунты списком, ручная запись с текстом объявления — раньше второй аккаунт был вне учёта вовсе); `142f356` — **сцены интерьера как данные** (18 сцен в четырёх группах вместо пяти, компоновка «частые сверху», редактор в ⚙ Настройках, блоки достоверности против «нейросетевого» вида фото). Два круга код-ревью, 16 находок, все исправлены до коммитов.
**Стратегическое решение Дениса:** версия для чужих ниш (обувь, ноготочки, инструменты) будет **ОТДЕЛЬНЫМ приложением**, а не сборкой из этого файла. ⚠️ До первого её запуска надо развести документы синхронизации и журнал — иначе форк сольёт своё сырьё в `voice` Дениса и испортит разбор.
**Не проверено живьём:** ротация (ждёт объявления, которому пора обновиться) и новые сцены.

🔴 **ТРИ МЕХАНИЗМА ПОТЕРИ ЗАПИСЕЙ ИСТОРИИ (найдены 14.09, исправлены).** Все три уносили записи МОЛЧА, мимо корзины:
1. **Близнецы по ключу-содержимому.** Личность записи считалась как `товар|регион|название|дата`, а повторные нажатия «✓ Размещено» создают записи с одинаковыми полями (дата — с точностью до дня). Удаление пустого дубля ставило надгробие на ОБЩИЙ ключ, и следующее слияние уносило по нему полноценную запись с финалом. Так пропали обе Сыктывкарские. **Лечение:** постоянный `_uid` каждой записи (миграция при запуске), ключ по содержимому стал ЗАПАСНЫМ и работает только там, где личности нет; снимок прежнего ключа хранится в `_ck0`, чтобы копия, уехавшая на сервер до миграции, опознавалась и не возвращалась двойником.
2. **Затирание из соседней вкладки.** `_saveAll` писал ключи целиком — побеждал тот, кто сохранил последним. Вкладка, пролежавшая 3 часа без дела, при первом же действии клала свой устаревший снимок и сносила всё, что завела соседняя. Так пропала Пермь: её создала вкладка B в 13:26, а вкладка A, открытая в 10:29 и ни разу не обновлявшаяся, перезаписала хранилище в 16:16. **Лечение:** сырьё (История, эталоны, избранное) пишется СЛИЯНИЕМ — `saveMerged()` читает хранилище и дописывает к своему набору чужое, с отсевом по кладбищу и перештамповкой совпавших id. Разбор идёт только когда строка отличается от записанной этой вкладкой, поэтому обычный `save` остался дешёвым.
3. **Удаление записи с финалом ничем не предупреждало** — вопрос был одинаковый для пустой строки и для пары «бот → правка». Теперь говорит прямо, с числом знаков.
➕ **ПРЕДОХРАНИТЕЛЬ НА СЫРЬЁ:** запись с непустым `finalDesc` не выбрасывается по надгробию-наследию (ключ по содержимому). Но явное удаление по ключу-личности уважается всегда — иначе строку нельзя было бы убрать вовсе.
⚠️ **Ловушка разбора, стоившая полсессии:** корзина и кладбище **не входят в файл-бэкап**, и я прочитал их отсутствие как «удалений не было» — вывод развернулся на 180°. При любом разборе сохранности смотреть `localStorage['avito_trash']`/`avito_deleted` или серверный `voice`, а НЕ экспорт.
✅ **Журнал оказался второй копией сырья** — событие `edited` несёт обе версии текста целиком. Из него восстановлены Пермь и Саратов, которых не было ни в одном бэкапе. Выгрузка живёт на сервере: `~/avito-rescue/finals.json` (5 пар), снимок всего хранилища — `~/avito-rescue/snapshot-20260913-134321/`.
⚠️ **РЕШЕНИЕ ДЕНИСА 12.09: промпт и структуру пока НЕ МЕНЯЕМ.** Его слова: «хочу проверять сырой промпт и делать руками всё без помощи от сырья, чтобы лучше работать». Его правки — это сырьё, выводы по нему будут точными позже. Поэтому правила SYS, пул приёмов и цены каталога **не трогать до его слова**, сколько бы находок ни давал разбор финалов.
✅ **Выключатель few-shot НЕ НУЖЕН — решение Дениса 13.09:** «сырой промпт проверять не будем». То есть сырьё подмешивается всегда (`buildMsg` → `learnedBlock`, `avito-helper.html:2657` — 5 примеров целиком, 12 настоящих заголовков, приёмы), и это принято как норма; посмотреть содержимое можно кнопкой «👁 Что уходит в промпт». Задача снята, не возвращать.
**Следующий шаг:** (1) Денис пробует ротацию на объявлении, которому пора обновиться, и новые сцены интерьера (перед проверкой **Ctrl+F5** — Pages кэширует). (2) Денис продолжает работу и доносит финалы; когда наберётся ~20 пар — разбираем, считая editShare **раздельно по телу и хвосту-подписи** (см. ниже). (3) Экспортная версия отдельным приложением — начинать с разведения документов синхронизации и журнала.
⚠️ **Главное правило проекта, заданное Денисом прямо:** сырьё (эталоны, финалы) и промпт — единственное невосполнимое, «из них можно слепить что угодно». Любая правка, способная их стереть, закрывается тестом до коммита. Подробности — в памяти сессий (`priority-raw-material-and-prompt`).
🆕 **Задача следующей сессии — пул приёмов (посчитано 2026-09-11 по бэкапу, цифры проверяемые).** В пуле **125 уникальных приёмов** из 42 разобранных объявлений, в каждый запрос уходят 4 случайных. У пула ДВЕ независимые проблемы, и лечатся они по-разному:
1. **Клише — 10 бесспорных + 6 спорных.** Бесспорные (давят на эмоцию вместо товара): превосходная степень в заголовке, волшебное преображение, страх упустить (ограничение функционала без покупки), хайповая подводка перед списком, слоган-обещание образа жизни, призыв отказаться от скуки, броский слоган-афоризм, провокационное противопоставление «старое/новое», конструкция «не просто X, а Y», риторический вопрос про мечту покупателя. Спорные (обычное ремесло продающего текста, решать Денису): эмоциональный крючок в заголовке, глагол-приглашение, погружение в атмосферу, обещание новых впечатлений, обращение к «искателю впечатлений», экспертное определение категории. Лечится удалением конкретных строк — **решение субъективное, за владельцем**.
2. **Перекос по темам — 46 приёмов из 125 на пять тем:** эмодзи-маркеры 14, тарифная сетка/лесенка 11, цена за месяц и сравнение с мелкой тратой 8, группировка по категориям 7, закрытие возражения о сроке 6. Больше трети пула, поэтому примерно каждая вторая генерация получает «поставь эмодзи-маркеры» или «сделай ценовую лесенку» — не потому что это лучший ход, а потому что таких формулировок больше всего; редкие и более интересные ходы вытесняются. Лечится **механически**: группировать приёмы по смыслу и брать 4 из РАЗНЫХ тем, а не 4 случайных из 125. Ручной разметки не требует и, вероятно, даст больше, чем вычистка клише.
⚠️ Обе правки независимы. Исходные оценки «~12 клише» и «5-6 дублей» были на глаз и оказались занижены — числа выше получены фильтром по корпусу и перепроверены.
**Прочие хвосты:** `OPENROUTER_AVITO_KEY` + живая проба `/api/image` (2б); 6 находок ревью 2б; идея отдельного «сканера клише» (ложится в `SCAN_RULES` уровнем «жёлтый»); масштабирование на другие товары (обсуждать отдельной сессией).

🆕 **Разбор первых трёх финалов (сессия 2026-09-12) — считать правки надо иначе.** `editShare` в текущем виде **завышает** правки, потому что дописанный постоянный хвост-подпись считается изменением тела:

| | слов бота → финал | editShare | по телу (без хвоста) | удержано слов бота |
|---|---|---|---|---|
| Wolverine | 132 → 205 (хвост 83) | 34% | **13%** | **84%** |
| PS+ Extra | 100 → 203 (хвост 82) | 61% | **48%** | 59% |
| Claude Pro | 76 → 223 (подписи нет) | 85% | — | 29% |

Вывод: **Денис не придирчив к текстам** — Росомаху принял почти целиком (84% слов бота дошли до финала). Большие проценты дают три дефицита ДАННЫХ, а не стиля: (1) **цены каталога устарели** — PS+ Extra бот дал 1400/3000/7300, реальные 1700/3800/от 6900; (2) **нет каталога фич AI-подписок** — в Claude Pro Денис дописал Cowork, Fable в X5, Opus/Sonnet, «в 5 раз больше сообщений», 2000 ГБ, бот писал по трём строкам формы; (3) **бот не знает про хвост-подпись** (придуман 12.09). Стилистических правок мало и они системные: убрать эмодзи-маркеры перед пунктами, тире `—` → дефис `-`, «оформляется в цифре» → «оформляется онлайн» И первой строкой, обращение к своим («наш любимый Логан»). ⚠️ При месячном разборе считать ТРИ числа: правки по телу, объём хвоста, **удержание слов бота** (LCS/|бот|) — иначе выйдет ложный вывод «переписывает половину».

🆕 **Хвост-подпись (стратегия Дениса с 12.09).** Изучил конкурентов, увидел сплошные нейротексты и ботов в ответах — и дописывает блок «зачем обращаться ко мне, а не к продавцу за N руб»: честная цена → «не продавец, а геймер, помогаю собратьям» → «живой человек, а не торговый бот» → репутация. ⚠️ **ПРАВИЛО ДЕНИСА: подпись НИКОГДА не копируется дословно — он переписывает её в каждом объявлении.** Его слова: «если ты не обратил внимания, подписи в объявлениях неидентичные. В третьем объявлении на другом профиле я тоже поменял подпись, и так всё время». Сверено по бэкапу — меняется и структура, и слова: «зачем писать мне» / «зачем обращаться ко мне», «159 руб» / «условно 1000 руб», «наслаждаться играми» / «получать удовольствие от игр», «Минутка радости» / «Маленькие радости», «которые вы редко встретите» / «которую вы нечасто встретите», «наводнены» / «переполнены». То есть антидубль-ротацию постоянного блока он делает вручную и сознательно — **это не то, что нужно «внедрить», это то, что нельзя сломать**: если блок когда-нибудь пойдёт в промпт, он должен идти НАБОРОМ заготовок (5-6 версий под 60+ объявлений), а не одним шаблоном. По моему разбору сократил хвост с 83 до 61 слова и **убрал фразу про «игра не будет полностью вашей»** — она ближе всего к запрещённой теме аккаунтов (слов-триггеров не содержала, но смысл читался). ⚠️ Важная поправка Дениса: аргумент честный — **у него игра остаётся полностью у клиента, а у конкурентов один доступ делится на 2-3 человека**; «бумеранга» нет, тему можно проговаривать в переписке, где правила площадки мягче. Следствие для промпта (когда дойдёт очередь): «читается как ИИ» — это ДЕФЕКТ, а не вкус, потому что именно этим Денис отстраивается; список 10 клише в пуле приёмов перестаёт быть субъективным.

🆕 **Сессия 2026-09-11 — сырьё: бэкап, синхронизация, голос.** Разобран настоящий корпус Дениса: **153 эталона** (111 ✍ своих, 42 👀 — все разобраны на приёмы), подписки 92 / игры 26 / AI 18 / физтовар 17, медиана описания 732 знака. Экспорт раньше клал только Историю и эталоны — теперь **полный бэкап v3** (плюс избранное, категории, пресеты, очередь, настройки; без токена). **Синхронизация**: `voice` сливается в обе стороны, `settings` пишется автоматически, а забирается по кнопке; удалённое уходит в **корзину** (возврат из ⚙ Настроек), в генерации не участвует. Сервер — `poigraem-backend@96ea8a4`, раскатан VPS-сессией по `TASK-AVITO-SYNC-VPS.md`, 10 проверок зелёные, **Денис отправил данные с боевой ссылки — работает**.
**Few-shot переделан под объём корпуса:** было 2 случайных примера, обрезанных до 220 знаков (модель видела ~1% сырья); стало — примеры **целиком**, схема «3 + 2» (три своей категории + гарантированно чужой слот A + добор B по всему корпусу), близость по **редкости слова в собственном корпусе** (иначе «ps5», стоящее почти везде, перевешивало тип товара), блок из 12 **настоящих заголовков**, кнопка «👁 Что уходит в промпт». **Финальные тексты с Авито впервые влияют на генерацию** — отдельный слот и пометка ранга; гарантия включается от двух финалов, чтобы единственный не лез в каждую генерацию.
**Метки эталонов теперь про ГОЛОС, а не про авторство** — поправка Дениса: 42 «чужих» это его же объявления, написанные нейросетью. «✍ мой голос» / «👀 не мой», переключается нажатием на строке.
⚠️ **Два круга код-ревью — 15 находок, все исправлены.** Пять из второго круга были потерей сырья: объединённый набор не уезжал обратно на сервер; финал не доезжал до второго устройства; метки удаления были вечными (заведённый заново эталон исчезал); пустой браузер затирал настройки на сервере; переключение голоса плодило дубли. Отдельно: кнопка «↑ Отправить сейчас» рапортовала об успехе всегда, даже когда запроса не было. **Урок: код синхронизации, написанный ради сохранности, сам оказался её главным риском — ревью обязательно.**
**Стенд: 664 проверки, 12 секций** (новые `backup` 59 и `sync` 66; `fewshot` вырос до 111).

**Сессия 2026-09-10 → 09-11 — направление 2б: код готов и раскатан, вопрос двух репозиториев решён.** (Дополнено 13.09: ключ заведён, модель выбрана, имена параметров исправлены — см. блок сессии 2026-09-13 выше.)
**Bug #9 закрыт в корне.** Выяснилось: `/api/openrouter` не «терялся при `git pull`» — его вообще НИКОГДА не было в репозитории (проверено локально: слова `openrouter` не было ни в `poigraem/backend/`, ни в `poigraem-backend/`). Значит, его когда-то дописывали руками на боевом сервере, и перезаливка каталога его стирала. Теперь код живёт в репозитории **`poigraem-backend`** — и заодно **решён вопрос «какой репозиторий бэкенда главный»**: по `poigraem-backend/DEPLOY.md` и его же `CLAUDE.md` источник истины — репозиторий `poigraem-backend`, а `poigraem/backend/` — застывшая копия, которую PM2 НЕ исполняет (правки, положенные туда, на прод не попадают). ⚠️ Первая версия эндпоинтов была ошибочно написана в мёртвую копию `poigraem/backend/server.js`; отменено (`git checkout`), переписано по боевому файлу.
**Серверная часть — задеплоена, ДВА круга ревью VPS-сессии, живой HEAD `af25ed4`** (`poigraem-backend`; ⚠️ `git pull` перед любой правкой маршрутов): `POST /api/openrouter` (текст, модель из браузера, потолок `max_tokens`, `n`→1, `stream` снимается, свой 400 на пустые `model`/`messages`) и `POST /api/image` (картинки; модель ЗАШИТА в `OPENROUTER_IMAGE_MODEL`, дефолт `openai/gpt-image-2.5-sunburst`, браузер её не выбирает; белый список параметров, `n`/seed серверные). Ключ **`OPENROUTER_AVITO_KEY`** (отдельный от общего у соседей). Нет ключа → **503** (не 401/404: клиент чистит токен на 401). ⚠️ Имена параметров с 13.09 — **`aspect_ratio`/`quality`, в единственном числе** (живой HEAD `fbc82cc`); множественные, с которыми маршрут был написан, вендор молча выбрасывал.
⚠️ **Поведение маршрутов после 2-го ревью — важно для клиента (сейчас клиент шлёт 1 фото ~150 КБ, всё под лимитом, но при доработке учесть):** у `/api/image` СВОЙ лимит тела **4 МБ** (`OPENROUTER_IMAGE_BODY_LIMIT`), не 10 МБ — тело размножается по памяти ~6 раз, процесс под `max_memory_restart: 300M` делит их с `crosspost.js`; значит одно-два фото, не три, и считать base64 (~1.4× оригинала) ДО отправки. Таймаут вендора → **504** (можно повторить), не 500 (сломались мы). **413** теперь JSON `{error, limit, got}`, не HTML. Правки ревью VPS (детали — серверный журнал `poigraem-backend/CLAUDE-VPS.md`): `relayUpstream()` против HTML-ответов Cloudflare, `AbortSignal`-таймауты, `envInt()`, guard `req.body`, плюс три регрессии от первого круга (сломанный `ecosystem.config.js` не поднимал pm2, `.env` через `__dirname`, `return` без `await`). **На будущее (после живой пробы):** переделать `/api/image` на сквозной поток вместо буферизации — память перестанет зависеть от размера, лимит поднимется. Журнал серверной части — `poigraem-backend/CLAUDE-VPS.md` (серверные сессии) и `CLAUDE-LOCAL.md` (Windows-сторона).
**Клиентская часть — закоммичена (`2b9ac33`), стенд 478/478.** В `avito-helper.html` (+248): карточка «🏠 Товар в интерьере» внутри карточки распознавания (5 сцен + «своими словами»), `stagedPrompt()` (сцену задаёт человек, товар переносится с фото БЕЗ изменений), `doStagedPhoto()` → `PROXY_IMAGE`, `stagedExtractImage()` (терпимый разбор ответа — форма ответа вендора живьём НЕ подтверждена), перенос в обложки/скачивание, `freshVision()` (свёл дублирование состояния vision). Новая секция стенда `tests/staged.mjs` (74 проверки).
✅ **Находки код-ревью 2б: пять из шести закрыты сессией 2026-09-13** (`avito-helper@1c64af0` + `3656487`) — тексты и чем закрыта каждая в `AUDITS-ARCHIVE.md`. Коротко: №2 и №4 (порча канваса удалённым URL и её корень) сняты вместе с самими `url`-ветками — замер четырёх моделей показал, что маршрут отдаёт только `b64_json`; №1 (незаэкранированный `src`) закрыт с ОБЕИХ сторон — белый список `media_type` и проверка `b64_json` по алфавиту base64 плюс `esc()` на стоке; №3 (атрибуция журнала) — снимок категории до `await`; №6 — `phase:'generate'` и `staged` в таксономии `tools/journal-report.mjs`.
⚠️ **ОСТАЁТСЯ ОДНА, 🟡 №5 — `stagedToCovers` дублирует `visionToCovers`** почти дословно (game-confirm, лимит 10, `new Image`→push+jlog+tab+render, onerror-toast); различие — текст, `source` в jlog и какой dataUrl. Свести в общий хелпер `pushImageToCovers(dataUrl, {source, extra})`. Косметика, работе не мешает.

🆕 **Сессия 2026-09-09 — санитарный срез журнала + закрыты все 9 отложенных находок.** Срез (данные с VPS + чтение кода) показал: **техника здорова** — эндпоинт жив, телефон и ПК стекаются в один файл, дублей 0, потерь 0, битых строк 0, ретеншн до марта 2027. Три «пропавших» события объяснены без единого дефекта: `refs_tricks` нечего было разбирать (ноль `reference_added`), а чипы (`febdbea`, 07.09 01:01) появились ПОЗЖЕ, чем были записаны 30 из 46 событий (06.09, машина разработки, 7 сборок за вечер); при этом confidence был high×5/medium×3, а промпт при видимом признаке варианты давать запрещает — пустой `uncertain` значит, что порог работает. Механика чипов подтверждена стендом (37/37).
⚠️ **Главный вывод среза — другой: за неделю ноль `copied`, `posted`, `fav_add`, `edited`.** Денис инструментом ещё НЕ РАБОТАЛ, только проверял правки; все 46 событий — следы разработки. Поэтому: **месяц до разбора считать от первого дня реальной работы, а не от 01.09**, иначе разберём три недели тишины. Тестовые события убраны с сервера (файл переименован в `archive-devtest-2026-09.jsonl`, вне маски уборки), новый месячный файл начнётся с чистого листа.
**Закрыты все 9 отложенных находок журнала** (были в разделе ниже): клиент — пауза после неудач (`_jOldestAt` сбрасывался только при пустом буфере, поэтому после 30с ошибок POST уходил на КАЖДЫЙ клик; теперь 5с→5мин, `force` её игнорирует), ключ идемпотентности `eid`, гард нетерпеливых кликов через `jActBusy(act, el)` (знает флаг каждого действия, а не только `state.loading`), `lsRaw()`; сервер — `AVITO_JOURNAL_ENABLED`, потолок события 32КБ с подменой заглушкой, троттлинг уборки раз в час, убран молчаливый `slice(0, 500)`. Плюс `/code-review` дал 5 находок: исправлены все, включая **важную в моём же анализаторе** (считал `editShare` вместе с доносами без вставленного описания, где ноль возникает ПО ПОСТРОЕНИЮ — медиана утонула бы в структурных нулях и дала вывод, обратный правде) и **не входившую в дифф**: `doRephrase` не имел замка, а кнопка «↺ Перефраз» лежит вне блока со спиннером — двойной тап на телефоне оплачивался ДВАЖДЫ. Стенд **404/404**, секция `journal` — 40 проверок. Появился `tools/journal-report.mjs` — разбор журнала на 10 секций, готов к месячному анализу.
⚠️ **Найден механизм Bug #9 в чистом виде:** боевой `~/poigraem-backend/server.js` имеет СОБСТВЕННЫЙ origin — репозиторий `poigraem-backend`, отдельный от `poigraem`, где лежит `backend/server.js`; файлы разошлись на 67 строк. Правка журнала закоммичена в оба (магазин — `911531d`, боевой — VPS-сессией) и раскатана патчем. **Вопрос «какой репозиторий бэкенда главный» сознательно оставлен на потом** — но пока он открыт, любая правка сервера должна попадать в ОБА, иначе `git pull` её затрёт, как затёр `/api/openrouter` в июле.

🆕 **Сессия 2026-09-06 → 09-07 — направление 1 доведено до конца + самообучение на эталонах.** 12 коммитов (`d230736`…`37beb85`), все запушены. Категория стала ДАННЫМИ: `CATS` пересобирается из встроенных `BUILTIN_CATS` и оверлея в `localStorage['avito_cats']`; встроенные правятся патчем-отличием, а не копией, поэтому «сбросить к заводской» = удалить патч, а будущие правки заводских категорий доезжают до Дениса, а не замирают его копией. В ⚙ Настройках появился редактор категорий (создать/клонировать/удалить, поля со стрелками порядка, скрытие, экспорт/импорт JSON для переноса между телефоном и ПК). Категория теперь управляет всей цепочкой: `SCAN_RULES` — 10 именованных проверок, и **одни и те же галочки правят Сканер И запреты в SYS-промпте** (рассинхрон уровня F19 стал невозможен по конструкции); распознавание по фото собирает список категорий и полей из дескрипторов; `currentProductName`, инфографика и подстановка рыночной цены пошли через флажки полей «название товара» / «цена товара». Добавлена 5-я встроенная категория **«🧷 Физический товар»** — собрана целиком на движке, своих веток в коде нет: 9 полей, свои правила (слова оплаты разрешены, платформа и «оформляется в цифре» не требуются, цена в тексте запрещена как у игр), правило промпта «пиши языком продавца вещей».
**По живой проверке Дениса** добавлено: запрет штампа «Продаю/продам/продаётся» (правило `selling_word` — Сканер + SYS + галочка в редакторе); распознавание версии товара «сначала признаки, потом название» (PS5 Pro определяется уверенно, признак — наличие вентиляционных прорезей, а не их количество); **уточняющие чипы** — модель возвращает только то, что физически неразличимо на фото, человек тыкает вариант, пары «предложено → выбрано» идут в журнал (порог узкий по логике скоринга обложек магазина: метка по факту, а не по ощущению); эталоны подключены к генерации — **✍ свои** идут текстом, **👀 чужие** разбираются кнопкой «🔎 Разобрать приёмы» и подмешиваются только ходами, без чужого текста и фактов, с прогоном приёмов через тот же фильтр запретов, что и объявления. Образцы перетекают между категориями: своя в приоритете, чужая — с пометкой «перенимай тон, не формулировки», так что игры, которые Денис начнёт вставлять, помогают физтовару сразу.
**Регрессионный стенд переехал в репозиторий** (`tests/`, `cd tests && npm test`) — с 129 до **364 проверок** в 7 секциях; паритет сравнивает с `git HEAD` байт-в-байт промпт, валидацию, SYS и вердикты Сканера. Стенд поймал 5 собственных дефектов до Дениса, но пропустил конфликт имён `visionPick` — фича сломала загрузку фото, поймал Денис; закрыто тестом на настоящий `setInputFiles`. Пройдено два круга `/code-review` (6 + 4 находки, все исправлены).
Также в начале сессии обсуждён вопрос Дениса про ключ OpenRouter для направления 2б: у чат-бота и editorial-бота на VPS уже есть живой `OPENROUTER_API_KEY` (не `OPENROUTER_KEY`, как ошибочно было записано в этом файле — поправлено в разделе «AI-провайдеры»), но оба гоняют через него только ТЕКСТ — image-модели с этим ключом ещё никто не вызывал. Решено: отдельный платёжный ключ для инструмента заводить не сейчас, а когда дойдём до 2б, и с двумя ограничениями сразу — лимит кредитов на самом ключе + эндпоинт на VPS, который физически пропускает только одну зашитую image-модель (не браузер выбирает модель).
Сессия 2026-09-06 (vision) — **реализовано направление 2а плана сближения с Айдентикой: распознавание товара по фото**. Карточка «📷 Распознать по фото» в форме генератора: фото → категория и поля формы (для физтовара — состояние и комплектация строго по снимку, промпт запрещает додумывать), рядом «💰 Узнать рыночную цену» веб-поиском отдельной кнопкой, «🖼 → в обложки» и «↩ Вернуть форму». Правок на VPS не потребовалось — прокси пересылает тело как есть, фото ужимается в браузере до 1568px/JPEG. Заодно **весь инструмент переведён на 5-е поколение моделей**: Sonnet 5 (дефолт, новее и дешевле прежнего Sonnet 4.6), Opus 5, Haiku 4.5; введена таблица `MODEL_CAPS`, потому что `temperature` на новых моделях возвращает 400 — из-за этого выбор «Opus 4.7» в настройках раньше гарантированно ронял генерацию. Распознавание и поиск цены идут с adaptive thinking. Пройдены два круга код-ревью (7 находок в vision + одна в чужом коде импорта эталонов, все исправлены), итог — **93/93 headless, консоль чистая**; всё запушено (`9289065`, `29cb7fe`, `1042b9a`, плюс `501dd9d` — заметки второй сессии). **Денис подтвердил на живом инструменте: распознаёт.** Это закрывает и висевший с 13.08 вопрос, работает ли связка Pages → прокси → Claude с настоящим токеном. Следующий шаг плана — направление 2б (staged-фото), блокер прежний: `/api/openrouter` (Bug #9).
Сессия 2026-09-01 → 09-06 — **запущен журнал самообучения** (skill `bot-selflearn-log`, адаптированный с бота на инструмент): клиент логирует все пути генерации, выборы и копии, избранное, размещения, графику и перепись используемых функций; события уходят батчами на VPS (`POST /api/journal`, раскатан и проверен снаружи), телефон и ПК стекаются в один журнал. Закрыта главная слепая зона — финальный текст, который Денис правит уже в форме Авито: необязательный диалог после «✓ Размещено» + кнопка «📥 финал» в Истории задним числом, считается доля изменённых слов. Добавлена карточка **«📥 Эталоны»** (идея Дениса) — объявления, написанные руками, и чужие удачные, для сравнения языка с генератором. Пройдено два круга код-ревью (9 + 11 находок; исправлено 9 + 7, остальное осознанно отложено и записано ниже), попутно найден и исправлен **Bug #11** в самом приложении. Итог проверки — 81/81 headless, консоль чистая; всё закоммичено и запушено (`19b6fdc`, `57c96d2`, `66d8533`, `d6cc717`). **Главное на будущее: через месяц эксплуатации — разбор журнала** (методика в разделе «Журнал самообучения», обязателен дедуп по `ts + device + event`). Направление 2а (vision) реализовал коллега — коммит `9289065`, эта сессия его не трогала; 2б (staged-фото) по-прежнему упирается в отсутствующий `/api/openrouter` (Bug #9).
Сессия 2026-08-29 — по запросу Дениса проанализирован сторонний сервис [aidentika.com](https://aidentika.com) (AI-генератор товарных карточек для маркетплейсов: фото товара → AI-сцена → инфографика, плюс AI-примерка и AI-видео) и предложен план сближения. Ключевой вывод анализа: Айдентика — только визуал (текстов не генерирует, по обзорам это её ограничение), тогда как наш инструмент сильнее всего именно в текстах — то есть мы закрываем одну конкретную недостающую половину (визуал), а не догоняем по всему фронту. Денис выбрал (`AskUserQuestion`) начать с **инфографики в канвасе** — реализована и headless-проверена (38/38, Playwright, консоль чистая, скриншот отправлен Денису): новая карточка «📋 Инфографика» в конструкторе обложек (оба режима — Игра и Коллаж), стиль «пилюли-строки», кнопка «⚡ Заполнить из формы» (по категории товара, для игр — без цены, тот же принцип, что в сканере запретов), включена как ось антидубля в «✨ 4 варианта» (сторона/цвет/порядок строк), drag мышью и тачем, персист. Заодно закоммичены и запушены отложенные правки прошлой сессии (диагностика Bug #10, `.gitignore`) вместе с новой фичей — коммит `9cc9c53`; попутно обнаружено и исправлено: git identity не была настроена в этом репозитории для `git.exe` из GitHub Desktop, установлена локально той же личностью, что в истории коммитов. **Следующие шаги плана сближения (не начаты):** направление 2а (vision-распознавание товара по фото через уже работающий `/api/claude`) → направление 2б (генерация staged-фото «товар в интерьере», упирается в отсутствующий `/api/openrouter` — Bug #9).
🆕 Сессия 2026-08-13 — **восстановлен хостинг инструмента, генерация снова работает**. Денис сообщил об «Ошибка: Failed to fetch» при генерации; диагностика реальным браузером показала, что приложение исправно, а запрос блокируется CORS ещё до отправки: прокси на VPS (`poigraem/backend/server.js:25-43`) отдаёт `Access-Control-Allow-Origin` только 7 доменам из whitelist, а файл, открытый с диска, шлёт `Origin: null`, которого в списке нет. Первопричина — не безопасность бэкенда, а коммит `9b642f6` (13.07) в репозитории магазина: он вынес avito-helper из публичного репо poigraem (`Standalone/avito-helper/`) в отдельный репозиторий без remote, и рабочая ссылка на GitHub Pages, которой Денис пользовался, умерла (обе старые ссылки → 404, проверено). Ключевое наблюдение: whitelist сверяет ДОМЕН, а не путь, а `https://enterteinment42.github.io` в нём уже есть — значит любой Pages-сайт под этим аккаунтом разрешён и правок на VPS не требуется вообще (подтверждено preflight-запросом: ACAO возвращается). Выбор Дениса (`AskUserQuestion`): отдельный публичный репозиторий + GitHub Pages. Сделано: `index.html` (редирект на `avito-helper.html` ради короткой ссылки), `.nojekyll`, `.gitignore`; раскладка Pages проверена локально имитацией подпути; Денис завёл remote, опубликовал репозиторий (сначала приватным — бесплатный Pages так не работает, затем сделал публичным) и включил Pages с `main` / `(root)`. **Живая ссылка: https://enterteinment42.github.io/avito-helper/** — проверена настоящим браузером: страница открывается, приложение монтируется, все 6 вкладок на месте, оба самохостных шрифта загружены, `/igdb/cover` отдаёт обложку (200), `/api/claude` с намеренно неверным токеном отвечает 401, т.е. запрос ДОХОДИТ до сервера и CORS пройден (реальную генерацию не гонял, чтобы не жечь платные токены). Подтверждения Дениса, что генерация с настоящим токеном отработала, пока нет.
🆕 Сессия 2026-07-16 — по прямому запросу Дениса добиты последние 3 открытых пункта из хвоста аудитов: **F19** (сканер синхронизирован с запретами SYS — «подарок», слова оплаты, «цена: N» как ярлык), **UX-11** (автоскролл к результатам на мобиле после старта генерации), **UX-13** (title-подсказка объясняет пороги счётчика длины «N/50»). По ходу проверки нашёл и исправил 2 собственных бага в первой версии F19-правки (не запушено, поймано тем же прогоном): `\w` не матчит кириллицу в JS-regex, и голая «Цена: N» без ₽/руб в игре раньше не ловилась вообще. Затем по прямой просьбе Дениса («проверь то, что ещё никто не проверял через локальный сервер/headless») найдены и прогнаны 2 пункта, которые раньше были верифицированы только чтением кода, а не браузером: **UX-9** (подтверждено рендером `formHTML()` — в форме реально нет поля-заголовка объявления ни в одной категории, закрытие обосновано) и **Bug #9** (впервые прогнан реальный `doGenerate()` с провайдером OpenRouter при 404 от прокси — приложение деградирует контролируемо: ошибка в UI, `loading` не залипает, токен не трогается на 404/чистится на 401, повторный вызов не ломается; сам баг — отсутствие эндпоинта на VPS — НЕ исправлен, изменилось только знание о том, что его следствия не крашат интерфейс). Итого 24/24 + 14/14 headless-проверок (Playwright, реальные функции приложения), консоль чистая. **Из хвоста аудитов остаётся открытым только GFX-11** (нужно согласовать оси рандомизации обложек с Денисом) и **Bug #9** (сам эндпоинт всё ещё отсутствует на VPS). Правки закоммичены (`8c3b5de`) по прямой просьбе Дениса. Push НЕ сделан — и не нужен: Денис уточнил, что у инструмента вообще нет удалённого репозитория, HTML пока чисто локальный, пушить сейчас некуда и не требуется.
🆕 Сессия 2026-07-15 — по прямому запросу Дениса реализован и headless-проверен весь оставшийся хвост из аудитов: **GFX-8** (touch-события канваса + touch-перестановка миниатюр), **GFX-9** (превью первым и липким на мобиле), **GFX-12** (простой «Стикер с ценой» удалён — выбор Дениса через AskUserQuestion), **GFX-13** (`document.fonts.ready` перерисовывает канвас), **GFX-14** (хитбоксы стикера/текста по факту размера, не заглушки), **UX-12** (самохостинг шрифтов — отдельные `.woff2` в папке `fonts/`, выбор Дениса), **UX-14** (переключатель AI встроен в форму генератора, выбор Дениса), **UX-15** (кнопка «➕ Ещё варианты» — отдельный блок, не замена набора, выбор Дениса), **Bug #5** (`queue-add-batch` source), **Bug #7** (перепроверен и подтверждён закрытым), **Bug #8** (`circ4d` — теперь настоящая раскладка с pinwheel-квадрантами, не мёртвый алиас). Три развилки с несколькими вариантами реализации (GFX-12, UX-14, UX-15) решены через `AskUserQuestion` — выбор Дениса зафиксирован в тексте находок выше. 24/24 headless-проверок прошли (Playwright, реальные функции приложения), консоль чистая (2 «мусорных» requestfailed от синтетических тестовых картинок без `.url` — не баг приложения). **Осталось из этого хвоста: F19** (сканер не ловит все текстовые запреты SYS) и **GFX-11** (нужно согласовать оси рандомизации обложек с Денисом до реализации) — GFX-11 сознательно не начат. Прежние хвосты: UX-11, UX-13, Bug #9, каталоги реальными играми, цены на подписки. Всё закоммичено (`099606d`, 18 файлов, включая новую папку `fonts/` и skill `.claude/skills/avito-helper-audit/`), но **визуальной приёмки Денисом нет**, и push пока некуда — у репозитория по-прежнему нет git remote.
**Блокеры:** нет
**Закрыто сессией 2026-09-06 → 09-07** (было в открытых вопросах): этапы 2-5 направления 1 сделаны; тест-драйверы коммитятся в репозиторий (`tests/`, решение Дениса) — вопрос «третий раз пишем набор заново» снят; 2 находки код-ревью этапа 1 (`select` без отката, неизвестный тип поля) исправлены; набор доп.полей физтовара вошёл в дефолтный шаблон категории; **распознавание после Sonnet 5 подтверждено живьём** («так себе» — устаревшая оценка: PS5 Pro определяется уверенно, комплектацию не выдумывает).
**Открытые вопросы:** ~~выключатель few-shot~~ — ✅ снят решением Дениса 13.09 («сырой промпт проверять не будем»); ⚠️ **корзина и кладбище не входят в бэкап** — удалённое живёт только в `localStorage['avito_trash']`/`avito_deleted` и в серверном документе `voice`. **Цена подтверждена делом 14.09:** по экспорту корзина выглядит пустой, и разбор инцидента пошёл по ложному следу, пока статус синхронизации не показал «в корзине: 3». Починка дешёвая (добавить оба списка в `backupPayload`), но не сделана; 🆕 **правка ✎ не расходится между устройствами** — слияние добавляет новые записи и подтягивает финалы, но не обновляет товар/регион в уже известной записи (Денис работает с одного устройства, поэтому пока не жмёт); 🆕 **анализатор журнала считает editShare одним числом** — нужно раздельно тело/хвост/удержание (`tools/journal-report.mjs`, см. разбор трёх финалов выше); 🆕 **`404` в консоли Дениса** при работе — URL не опознан, к ошибкам генерации отношения не имеет; 🆕 **заголовки: бот не добирает лимит и не даёт русского написания** (Денис правил все три из трёх: «Marvel's Wolverine → Росомаха», «контекст 200k токенов → умнейшая нейросеть») — править после его слова; 🆕 **заголовки физтовара слабые** — модель хватается за состояние («отличное», «как новая»), не варьирует написание названия, не добирает лимит 50 символов; правило-заплатка про крючки в категорию добавлено, но Денис прямо сказал: «тут так просто не получится, нужно сырьё и журнал самообучения» — настоящая доводка после месячного разбора; 🆕 живьём не проверены сами уточняющие чипы (на фото PS5 Pro признак был виден, поэтому чипы не показывались — это верное поведение, но UI выбора Денис ещё не трогал) и кнопка «🔎 Разобрать приёмы»; 🆕 не проверен на телефоне блок из 10 галочек правил категории; ~~приёмка Денисом журнала самообучения~~ — ✅ закрыта сессией 2026-09-12 реальной работой (три донесённых финала + два новых эталона), текст в `AUDITS-ARCHIVE.md`; ~~6 находок код-ревью 2б~~ и ~~живая проба `/api/image`~~ — ✅ закрыты 13.09 (осталась косметическая №5, см. блок 2б); ~~клиентский путь 2б живьём не ходил~~ — ✅ **закрыт 22.09: Денис прогнал фото консолей в интерьере, работает**; 🆕 **не проверены живьём ротация объявлений и новые сцены** (обе правки от 22.09); 🆕 **мультиарендность** — инструмент живёт в одном браузере под общим `ADMIN_TOKEN` и на ключе Дениса, продать клиенту в таком виде нельзя; экспортной версии нужно своё хранилище, свой журнал и учёт расхода; 🆕 **нужен ли режим «💎 Финал»** — Денис счёл `medium` достойным, разбор журнала покажет по доле генераций, дошедших до скачивания; актуальные цены на подписки; точный размер фото на Авито; оси рандомизации GFX-11 не согласованы с Денисом; подтверждает ли Денис предложенное закрытие UX-9 (headless-проверено сессией 2026-07-16, но не перенесено в архив без явного подтверждения; Bug #7 аналогично); хочет ли Денис лично проверить накопившийся стек (F1…F20, GFX-8…14, UX-9…15, Bug #5/#7/#8) на живом инструменте; отдал ли Денис ссылку второму человеку и нужен ли тому админ-токен; подтверждает ли Денис визуально фичу «Инфографика» (сессия 2026-08-29, приёмки нет); не пора ли пересмотреть промпты генерации под 5-е поколение моделей — они вылизывались под Sonnet 4.6.
**Настроено:** git commit из Claude Code через `git.exe` из GitHub Desktop (`C:\Users\enter\AppData\Local\GitHubDesktop\app-<версия>\resources\app\git\cmd\git.exe` — версия меняется с обновлениями GitHub Desktop, на 2026-08-29 это `app-3.6.4`; искать актуальную папку `app-*`). 🆕 Git identity (`user.name`/`user.email`) настроена локально в этом репозитории (сессия 2026-08-29) — `enterteinment42 <enterteinment42@gmail.com>`, той же личностью, что в прежних коммитах. **У репозитория теперь есть remote и публичный хостинг (сессия 2026-08-13):** `origin` → `https://github.com/enterteinment42/avito-helper.git`, репозиторий **публичный** (бесплатный GitHub Pages работает только с публичными; приватный + публичный сайт — только на платном GitHub Pro), сайт собирается с ветки `main`, папка `/(root)`. **Живой адрес инструмента: https://enterteinment42.github.io/avito-helper/** — открывается с любого устройства, эту же ссылку отдавать другим людям (для генерации им нужен админ-токен, запросы пойдут с ключа Дениса; обложки/сканер/экспорт работают и без токена). Прежнее решение «инструмент чисто локальный, remote не заводим» (сессия 2026-07-16) отменено — именно оно и было причиной поломки генерации, см. Bug #10. ⚠️ Теперь каждое изменение `avito-helper.html` нужно коммитить и пушить, иначе на сайте останется старая версия (Pages пересобирается 1-3 мин). ⚠️ Репозиторий публичный — все заметки (`CLAUDE.md`, архивы, skill) и системный промпт видны всем; паролей и ключей в них нет (проверено), а копии до 13.07 и так лежат в публичной истории репо магазина.
🆕 **Регрессионный стенд живёт в репозитории (сессия 2026-09-07, расширен 09-22):** `tests/` — Playwright, `cd tests && npm install && npm test`, **1121 проверка в 16 секциях** (с 23.09; `parity`, `editor`, `stage3`, `phys`, `review`, `uncertain`, `fewshot`, `journal`, `staged`, `backup`, `sync`, `truncate`, `history`, `rotate`, `covers`, `regress`; одну секцию — `node run.mjs <имя>`). ⚠️ Полный прогон занимает ~4 минуты и изредка падает по таймауту Playwright на холодном старте — повтор проходит целиком, это флак, а не регрессия. Рядом `tools/journal-report.mjs` — разбор журнала самообучения (не часть стенда, запускается отдельно; описан в `tests/README.md`). `node_modules/` и `.baseline/` в `.gitignore`. **Паритет сравнивает с `git HEAD`**, поэтому baseline едет вперёд с каждым коммитом: списки `EXPECTED_SYS_LINES`/`EXPECTED_SCAN_LABELS` в `parity.mjs` описывают отличия только от последнего коммита и **должны очищаться сразу после того, как изменение закоммичено** — иначе стенд вычитает строку, которая уже есть и в эталоне (эта ловушка сработала на правиле `selling_word`). Правило записано в `tests/README.md`.
✅ **ДВА РЕПОЗИТОРИЯ БЭКЕНДА — вопрос РЕШЁН (2026-09-11):** источник истины — репозиторий **`poigraem-backend`** (его чекаут = боевой `/home/ubuntu/poigraem-backend`, оттуда PM2 и запускает `poigraem-api`). `poigraem/backend/` внутри витрины — **застывшая копия**, которую PM2 НЕ исполняет; правки, положенные туда, на прод не попадают (это и был механизм Bug #9). Так постановил сам `poigraem-backend/CLAUDE.md` + `DEPLOY.md`. **Правило на будущее: серверный код правим ТОЛЬКО в `poigraem-backend`, копию `poigraem/backend/` не трогаем.** (Прежнее «класть в оба» отменено — оно лишь плодило дрейф.)

**Эндпоинты на VPS:** ✅ 🆕 **`/api/avito-sync/:doc`** (хранилище сырья, сессия 2026-09-11) — `GET`/`POST` под тем же `ADMIN_TOKEN`, белый список документов `voice` (эталоны, избранное, История с финалами) и `settings` (категории, каталоги, пресеты, очередь, оформление). Файлы `~/avito-sync/<doc>.json`, запись через временный файл с атомарным переименованием. Переменные `AVITO_SYNC_DIR` / `AVITO_SYNC_ENABLED` / `AVITO_SYNC_MAX_BYTES` (6 МБ). 🆕 **С 13.09 (`poigraem-backend@d9ef859`) вместо единственного `.bak` — каталог `history/` на 5 версий документа** (`syncRotate`, `SYNC_HISTORY_KEEP`); правка сделана после этого же инцидента и уже в git. Там же три находки ревью: битый документ отдаёт `{empty:true, corrupt:true}` (раньше спред `{...parsed}` от `null`/массива давал ответ без `data`, и клиент затирал локальное `undefined`); лимит считается в БАЙТАХ, а не символах (на русском разница 1.56×, «6 МБ» пропускало ~9.4 МБ); убран второй разбор многомегабайтного документа в ответе `POST`. ⚠️ Запись через **POST, а не PUT** — общая CORS-прослойка (`server.js:45`) не отдаёт `PUT`, и preflight из браузера не пройдёт; не «чинить». Раскатан VPS-сессией (`poigraem-backend@96ea8a4`, инструкция `TASK-AVITO-SYNC-VPS.md`), 10 проверок зелёные, **живая отправка с боевой ссылки подтверждена Денисом 11.09**. На сервере лежит `~/avito-sync-check.sh` с предохранителем: если документы уже есть (живое сырьё), пишущие проверки пропускаются — прямой прогон смоука затёр бы эталоны. ⚠️ Скрипт вне git, как и `~/bin`. ✅ `/api/journal` (журнал самообучения, сессия 2026-09-01) — задеплоен и работает; правки ревью раскатаны 2026-09-09 патчем и **проверены на бою реальным токеном**: запись `{"ok":true,"saved":1}`, батч из 3 доходит целиком (`saved:3` — молчаливой резки больше нет), событие на 40КБ ПОДМЕНЯЕТСЯ заглушкой `journal_oversize` (171 байт, `JSON.parse` проходит, поля `origEvent`/`size`/`device`/`eid` на месте, исходного payload внутри нет), права `644 ubuntu:ubuntu`, каталог `755`. Бэкап боевого файла — `~/poigraem-backend/server.js.bak-2026-09-09`; откат: скопировать обратно + `pm2 restart poigraem-api` (и откатить коммит `poigraem-backend@407c7d7`). 🆕 На сервере лежит **`~/journal-check.sh`** — прогоняет все проверки эндпоинта разом (запись, потолок события, целый батч, права), токен берёт из окружения и нигде не печатает: `ADMIN_TOKEN=$(grep -m1 '^ADMIN_TOKEN=' ~/poigraem-backend/.env | cut -d= -f2-) bash ~/journal-check.sh`. ⚠️ Живёт вне git (как и весь `~/bin`) — при переезде сервера потеряется. `/igdb/img` (прокси IGDB + PS Store, задеплоен) ✅ 🆕 **`/api/openrouter` (текст) и `/api/image` (картинки, 2б) — РАБОТАЮТ ЖИВЬЁМ** (сессия 2026-09-13, живой HEAD **`fbc82cc`**). Ключ `OPENROUTER_AVITO_KEY` в `.env` заведён владельцем 13.09, 503 больше нет. `/api/image` пропускает только зашитую в `OPENROUTER_IMAGE_MODEL` модель (дефолт и рабочий выбор — `openai/gpt-image-2.5-sunburst`) + белый список **`aspect_ratio`/`quality`** (единственное число! множественные дают свой 400 с подсказкой). Оба под `requireAdmin`. ⚠️ **Смена модели через `.env` — не всегда «одна строка»:** у `seedream`/`qwen`/`gemini` качество называется `resolution` (у seedream минимум 2K), у `flux` его нет вовсе; сверяться с `GET /api/v1/images/models` и править белый список. `/api/claude` теперь (2026-07-12, root-проект SEC-1) требует `Authorization: Bearer <ADMIN_TOKEN>` — `getProxyToken()` в `avito-helper.html` уже это учитывает, при первом запросе спросит токен и запомнит в `localStorage['avito_admin_token']`.

**Известные баги (не исправлены):**
- ~~**Bug #9**~~ — ✅ **ЗАКРЫТ ПОЛНОСТЬЮ, сессия 2026-09-13: живая проба прошла** (HTTP 200, настоящая картинка, `usage.cost` $0.0065). Текст в `AUDITS-ARCHIVE.md`. Коротко: эндпоинта физически не было в git — его дописывали руками на сервере, и перезаливка стирала; переписан в `poigraem-backend`, а 13.09 владелец завёл `OPENROUTER_AVITO_KEY`, и маршрут перестал отвечать 503.
- ~~**Bug #10 (найден и исправлен 2026-08-13)**~~ — «Ошибка: Failed to fetch» на любой генерации и на подгрузке обложек. Причина не в приложении: CORS-whitelist прокси (`poigraem/backend/server.js:25-43`, 7 разрешённых доменов — `poigraem.shop`, `poigraem.store`, `enterteinment42.github.io`, `igropolka.com` с вариантами `www`) не содержит `null`, а именно такой `Origin` шлёт браузер для файла, открытого с диска (`file://`) — запрос блокируется ещё до отправки, до сервера не доходит. Спусковой крючок — коммит `9b642f6` (13.07) в репо магазина, вынесший инструмент из публичного `Standalone/avito-helper/` в отдельный репозиторий без remote: ссылка на GitHub Pages умерла, остался только локальный файл. Исправлено восстановлением хостинга на разрешённом домене (отдельный публичный репозиторий + GitHub Pages, `https://enterteinment42.github.io/avito-helper/`), правок на VPS не потребовалось. Проверено живым браузером по боевой ссылке: `/igdb/cover` → 200 с обложкой, `/api/claude` с неверным токеном → 401 (раньше на этом месте было «Failed to fetch»). **Урок на будущее:** инструмент нельзя использовать как локальный файл — только с домена из whitelist; при любом переезде хостинга сверять `Origin` со списком в `server.js`.
- ~~**Bug #11 (найден код-ревью 2026-09-01, исправлен тогда же)**~~ — **действия на карточках «➕ Ещё варианты» (UX-15) всё это время применялись не к той карточке.** `parseVariants` нумерует ответ модели заново с 1, поэтому варианты из `state.extraResults` получали те же `id`, что и основной набор, а `findResult()`/`findResultRef()` ищут сначала в `state.results` — то есть ↻ (перегенерация), ☆ (избранное), 🔎 (дубли), 🔍 (сканер), ↺ (перефраз) и «✓ Размещено» на карточке из блока «Ещё варианты» молча срабатывали на одноимённой карточке ОСНОВНОГО набора: перегенерировался чужой вариант, в избранное/историю уходил чужой текст. В DOM при этом висели две карточки с одинаковым `#1`. Баг существовал с реализации UX-15 (сессия 2026-07-15) — тогда `findResult`/`findResultRef` сделали пул-осведомлёнными, но уникальность id не обеспечили. Исправлено в `doGenerateMore`: свежие варианты нумеруются дальше максимума пула (`resultsPool()`). Headless-проверено (R1: id не пересекаются, `findResult` отдаёт именно свой вариант, ☆ сохраняет верный текст). Найден при ревью журнала — журнал наследовал ту же ошибку в атрибуции копий.
- ~~**Bug #5**~~ — исправлено, сессия 2026-07-15: `queue-add-batch` теперь ставит `source:'batch'` (был неверный `'fav'` с `favId:null`), в строке очереди — метка «📦 пакет». Headless-проверено. Приёмки Дениса нет.
- ~~**Bug #6**~~ — исправлено сессия 2026-07-14 в составе F12 (`doRegen` теперь передаёт `webSearch` из снимка формы). Приёмки Дениса нет; из «известных багов» уберётся после подтверждения.
- ~~**Bug #7**~~ — ЗАКРЫТ, сессия 2026-07-15: перепроверено по живому коду (nav содержит «🗂 История», `dbTabHTML`/`bindDbTab`/`renderDb` рендерятся и используются) — мёртвого кода нет, был предложен к закрытию ещё сессией 2026-07-13.
- ~~**Bug #8**~~ — исправлено, сессия 2026-07-15: раскладка `circ4d` теперь настоящая пятая раскладка (не алиас `circ4`) — диагональные pinwheel-квадранты вокруг круга (`cvDrawCirc4D`), добавлена в `COLLAGE_UI_GROUPS`, `cvResolveLayout` резолвит её при 5 фото, hit-test через point-in-polygon (клик/drag/neon-обводка/выделение работают). Headless-проверено (резолв, cells, реальная отрисовка без исключений). Приёмки Дениса нет.


---

## Лог сессий

### 2026-09-25 — План сближения с Fotor: Ф1 буквы, Ф6 коллажи, Ф3 коррекция фото, Ф5 значки; четыре круга код-ревью

**Контекст:** сессия началась с вопроса «что у нас по плану сближения с Айдентикой» (ответ: закрыт 22.09) и
переросла в новый план — **сближение с Fotor по оформлению обложек**, только то, что нужно нашим объявлениям
и будущим клиентам. План — `PLAN-FOTOR.md`, ссылка в указателе и в «Открытых задачах».

**Что решил Денис:**
- Направления: Ф1 (текст), Ф3 (коррекция фото), Ф4+Ф5 (стиль и значки), плюс «посмотреть их коллажи» → Ф6.
  **Ф2 (слой-картинка, вырезка фона, тень под товаром) не взята.** Порядок: Ф1 → Ф6 → Ф3 → Ф5 → Ф4.
- Ф1: шрифты Montserrat Black, Oswald, Russo One, Rubik Bold; эффекты — на буквы всех слоёв с текстом.
- Ф6: узоры точки/полосы/клетка/соты; цвет узора — авто от фона + плотность.
- Ф3: мягко ±30 %; «Авто» выставляет ползунки; в коллаже — выбранное фото + «Ко всем фото».
- Ф5: набор Lucide (ISC); новые инфографики со значками, старые как были; отдельный слой «Значок» — да.
- Каждую часть: `/code-review` → исправить все находки с тестом → коммит и пуш.

**Что сделано — четыре коммита, все запушены, стенд 1203 → 1344:**
- `aa7e243` **Ф1, буквы слоёв:** 4 шрифта файлами в `fonts/` (грузятся при первом использовании, скачивание
  ждёт загрузки); блок «Буквы» у стикера/ленты/надписи/инфографики — шрифт, заливка цветом или градиентом,
  обводка (форма растёт под неё), тень (масштабируется со слоем). Попутно у JetBrains Mono объявлен
  **настоящий жирный 700** — файл переменный, холст раньше рисовал поддельный. Прежняя «тень у надписи без
  подложки» переведена в флаг слоя (`cvLayerCompat`) — старые надписи пиксель в пиксель как в HEAD.
- `b90fcf8` **Ф6, коллажи:** «Промежуток» и «Поле» раздельно (пересчётом в `cvCells`, фигурные раскладки
  сдвигом), «Углы» отдельно от шва, узор фона (и в рамке «Игры»), «Заполнить / Вписать целиком» у выбранной
  прямоугольной ячейки. Попутно: шов коллажа не сохранялся (рисовал без `cvSave`) и терялся на F5.
- `ae57b0f` **Ф3, коррекция фото:** карточка «🎛 Коррекция фото» — яркость, контраст, насыщенность, резкость,
  «Авто», «Сбросить», «Ко всем фото». Попиксельно, без `ctx.filter` (ненадёжен в Safari на iPhone).
  Исправленная копия кэшируется на картинке, предел 2000/4000/8000 px от увеличения кадра.
- `6ec0cf8` **Ф5, значки:** 28 значков Lucide путями Path2D (лицензия — в коде); эмодзи в начале строки
  инфографики — метка, на обложке значок цветом букв; переключатель «Значки», палитра «Вставить»; слой
  «⭐ Значок» (круг/квадрат/без фона).
- Во всех частях: при настройках по умолчанию обложки **пиксель в пиксель как в HEAD** — закреплено
  сравнением с baseline-страницей в секциях `letters`, `collage`, `icons`.

**Код-ревью: 4 круга, 8 находок, все исправлены до коммитов** (сводка — в `PLAN-FOTOR.md` по каждой части):
Ф1 — пустое кольцо тени при обводке, варианты не ждали шрифт; Ф6 — старые шаблоны не сбрасывали поле/узор,
узор обрывался на кольце и в рамке; Ф3 — удаление фото переводило коррекцию на соседнее, копия мылилась при
увеличении; Ф5 — палитра вставляла в первую строку (меню пересобирается, курсор теряется), имена вроде
«toString» роняли рисование.

**Дефекты, пойманные до Дениса:** первая версия «Авто» делала тёмное фото ТЕМНЕЕ — контраст вокруг 128
утаскивал тени; поймано скриншотом, контраст теперь вокруг средней яркости фото. «Авто» насыщало почти серое
фото (усиливало шум камеры) — теперь нет.

⚠️ **Инцидент сессии:** массовая замена скриптом PowerShell через `[IO.File]::WriteAllText` после `cd ..`
прочитала пустоту (у .NET другая текущая папка) и **записала 0 байт в `avito-helper.html`**. Незакоммиченная
Ф6 пропала из рабочей копии; восстановлено из `git HEAD`, правки наложены заново по контексту, секция
`collage` (писалась до инцидента) прошла целиком — восстановление точное. Сырьё не пострадало. Правило в
памяти: файлы проекта — только Edit/Write, не .NET из PowerShell.

**Новые баги (в коде до сессии):** шов коллажа не сохранялся на F5 (исправлен); на телефоне у вкладки
«Обложки» горизонтальная прокрутка — ширина страницы 882 px при экране 390, есть и в HEAD, не исправлено.

**Открытые вопросы:** Ф4 — логотип картинкой (простой слой PNG) или только текстовой подписью; утолщать ли
линию значков рядом с Montserrat Black; не мылит ли резкость 10 % у «Авто» шум на настоящих фото.

**Идея Дениса — серия фото в 2б** (записана в «Идеи» указателя): до 5 своих фото с разных ракурсов → по
картинке на каждое; одна комната на серию (первая картинка — второй референс, у вендора не проверено);
перед запуском — число генераций и цена. **Делаем после Ф4.**

**Что осталось:**
1. Денису — Ctrl+F5 и проверить живьём Ф1, Ф6, Ф3, Ф5 (плюс висящее с 22–24.09: слои и шаблоны, обложки
   и коллажи, ротация и сцены).
2. Ф4 плана Fotor — фирменный стиль аккаунта (решить про логотип).
3. Серия фото в 2б — после Ф4.
4. Пул приёмов — переносится четвёртую сессию подряд.

### 2026-09-24 — Обложки: слои оформления вместо трёх блоков, ручки на холсте, шаблоны; код-ревью 5/5

**Контекст:** Денис попросил навести порядок в графике обложек: тексты должны помещаться в пилюли стикеров, стикеры/тексты/инфографику крутить прямо по клику, а не ползунками, плюс шаблоны оформления («подтянул обложку, включил шаблон — в правом углу ленточка «предзаказ»»). Пул приёмов (задача, назначенная на сессию) снова не начат.

**Диагноз до правки (по коду):** текст стикера подгонялся под заранее заданный размер формы — длинная надпись в пилюле мельчала до нечитаемой; инфографика срезала строку по буквам без многоточия; поворот и размер — только ползунки 0–360° и px, у инфографики поворота не было вовсе; каждого элемента ровно по одному; значки платформ — только в четырёх углах; «лента» была полосой, повёрнутой на 45° вокруг центра стикера, а не угловой лентой; шаблонов нет.

**Что решил Денис (три вопроса):** слои, а не точечная докрутка; шаблоны — готовые + свои; делать сейчас, в монолите, до разбиения на модули (разбиение перенесёт готовый модуль дословно). Код-ревью — чинить все 5 находок «осторожно и без потери ценного сырья».

**Что сделано — коммит `981e44c`, запушен, стенд 1121 → 1203:**
- **Модель слоёв** `cv.layers`: `{id, type, x, y, angle, scale, hidden, …}`, типы sticker / ribbon / text / infog / plat. Центр — в долях холста (4:5 ↔ 1:1 не уносит за край), координаты больше не пиксели. Константы модуля стоят ДО `cvLoad()` — миграция идёт при запуске, а `const` в мёртвой зоне до объявления.
- **Форма под текст:** `cvStickerGeom` держит шрифт от размера, пилюля/бейдж тянутся вширь, круг/взрыв/соты/ромб растут и подбирают перенос по словам на минимальный описанный круг. `cvInfogGeom` — плашка по строке, блок шире 92% холста уменьшает шрифт, строка целиком. `cvRibbonGeom` — лента в углу, середина на диагонали на расстоянии m, внешняя хорда `2m − t` вмещает надпись с полями.
- **Ручки на холсте:** белая крутит (прилипание к 0/45/90°, Shift — 15°), жёлтая — размер, колесо/щипок тачпада над элементом — размер, два пальца на телефоне — размер и поворот; перенос прилипает к центру с направляющими; Del, Esc, стрелки; двойной щелчок — к тексту в меню. Рамка рисуется только на `#cv-canvas` — в JPEG не попадает. Лента тянется в угол той четверти, куда тянешь.
- **Меню:** карточки «🎨 Шаблоны оформления» и «🧩 Элементы на обложке» (добавить, список со 👁, ▲▼, ⧉, 🗑, редактор выбранного) — выше «Эффектов». Поля выбранного слоя — делегированием на документ (`data-cv-lp/lv/lt/lplat/la/tpl`), карточка перерисуется без своих слушателей. Платформы убраны из «Эффектов» в слои.
- **Шаблоны:** `CV_TPL_BUILTIN` — Предзаказ, Новинка (ленты), Хит, На русском, Карточка товара (инфографика и платформы из формы генератора, значки в правом нижнем — в левом верхнем наезжали на название игры), Без элементов. Свои в `cv.tpls`: слои + фон/эффекты/рамка (`CV_TPL_STYLE_KEYS`); применение ЗАМЕНЯЕТ слои, прежнее — в «↶ Отменить». Инфографика, заполненная из формы и не тронутая, сохраняется как `fromForm` — иначе шаблон таскал бы строки одного товара на все следующие.
- **Миграция прежнего формата** (`cvApplyCfg` + `cvLegacyLayers`) на всех трёх входах: localStorage, сервер (применение документа настроек), импорт бэкапа. Выключенные надпись и инфографика со своим текстом переезжают скрытыми слоями.
- Журнал: событие `cv_tpl`, счётчики `layers`/`tpls` в `cv_download`, библиотека шаблонов в снимок настроек не пишется; `cv_tpl` внесён в таксономию `tools/journal-report.mjs`.

**Код-ревью: 5 находок, все исправлены до коммита** (тексты — `AUDITS-ARCHIVE.md`). Главная: «↶ Отменить» держало снимок до нажатия и молча стёрло бы всё сделанное после удаления/шаблона, включая фон. Теперь отмена запоминает отпечаток «сразу после» и гаснет при любой следующей правке.

**Новые баги (в коде до сессии):** не найдено; перечень дефектов прежних блоков — в «Диагнозе» выше, все закрыты заменой.

⚠️ **Известное ограничение:** перевод старых настроек идёт до загрузки JetBrains Mono, ширина инфографики меряется запасным шрифтом — перетащенный блок один раз может сдвинуться на ~10 px. В стенде допуск 20 px с комментарием.

**Проверено:** стенд 1203/1203 (новая секция `layers` — 82); скриншоты ПК и телефона (390 px) осмотрены. **Живьём Денисом не проверено.**

**Открытые вопросы:** свои шаблоны едут в документе настроек — «последний пишущий побеждает», как у пресетов; GFX-11 (оси антидубля «4 вариантов») теперь можно расширить формами и позициями слоёв — не обсуждалось.

**Что осталось:**
1. Денису — Ctrl+F5, проверить слои и шаблоны живьём, что прежний стикер/текст/инфографика переехали; плюс висящие обложки/коллажи 23.09, ротация и сцены 22.09.
2. Пул приёмов — снова перенесён.
3. Прежние хвосты: корзина в бэкапе, раздельный `editShare`, находка №5 2б, GFX-11, разбиение на модули (модуль обложек стал больше — ~1000 строк новых).

### 2026-09-23 — Обложки: кадр, пропорции превью, прокрутка; коллажи: перетаскивание кадра, неоновый шов, инфографика за краем

**Контекст:** сессия началась с жалобы Дениса на обложку FC 27 в режиме «Игра»: верх обрезан, сдвинуть нельзя, 4:5 «приплюснут», превью уезжает за длинным меню. По ходу Денис прислал ещё четыре дефекта коллажей скриншотами. Пул приёмов (задача, назначенная на эту сессию) не начат.

**Что сделано — три коммита, все запушены, стенд 1067 → 1121:**
- `edf73b9` — **обложки.** Причины: (1) `cvFitSrcRect` ограничивал сдвиг запасом от зума, на 100% запас ноль — кадр стоял намертво, а режим cover срезал ~6% сверху и снизу портретной обложки в 4:5 (в квадрате ~15%), то есть ровно название игры; (2) «сплющивание» — не рисование, а вёрстка: `#cv-canvas` во флекс-колонке растягивался по ширине (`align-items: stretch`), а `max-height` урезал только высоту → 4:5 превращался почти в квадрат; (3) липкое превью было выше экрана, низ открывался только после конца меню. Сделано: сдвиг ограничен всей картинкой; режим «Кадр: Заполнить / Вписать целиком» для игры, поля — размытая копия обложки или цвет фона (`gameFit`/`gameFill`, персистятся); `align-self: flex-start` у холста; на ПК у меню и превью своя прокрутка высотой в экран, позиция переживает `render()`; масштаб колесом/свайпом/щипком тачпада пропорционально дельте (раньше фиксированный шаг на каждое событие — тачпад прыгал 100% → 400% за жест); попутно — панель масштаба в «Игре» пропадала после любого `render()` (шаблон её не содержал, заполнял только `cvRenderState`).
- `50107a0` — **коллажи.** По выбору Дениса (вариант Б из трёх): перетаскивание по холсту двигает кадр ячейки, поменять местами — только миниатюрами. Неоновый шов обрезан по центральной фигуре (circ4, circ4d, square5) — швы фоновых ячеек рисовались поверх круга белым крестом. Позиции инфографики, стикера и текста ограничены холстом (`cvClampOverlays`). Раскладка «Рамка» (`frame5`) удалена по слову Дениса, сохранённый выбор → авто. Треугольники circ4d переведены с `cvFitClip` на `cvFit` — сдвиг и масштаб там игнорировались.
- `1669f90` — рисование коллажа разрешает раскладку по числу картинок так же, как `cvCells` (находка ревью, вариант А).

**Инфографика «не работала» — диагноз подтверждён Денисом:** блок, утащенный вниз в 4:5 (высота 1350), в квадрате (1080) рисовался за краем; кнопка «Справа» сбросила позицию, и плашки появились. Координаты перетаскивания персистятся в `avito_cv` и ничем не ограничивались.

**Код-ревью: два круга, 2 находки, обе исправлены** (тексты — `AUDITS-ARCHIVE.md`).

**Что решил Денис:** «Вписать целиком» и сдвиг на 100% — оба; прокрутка — своя у каждой колонки; перетаскивание в коллаже двигает кадр, перестановка — миниатюрами; «Рамку» удалить; несовпадение раскладки и числа фото — рисовать «авто».

**Новые баги (в коде до сессии):** панель масштаба пропадала после `render()`; треугольники circ4d без transform; рисование и `cvCells` расходились при числе фото ≠ 5; неограниченные позиции оверлеев; флекс растягивал холст. Все исправлены.

⚠️ **Урок стенда:** `render()` пересоздаёт `#cv-canvas` — контекст, взятый до него, читает старую картинку. Проверка «шов не просвечивает» сначала прошла ложно именно так; ловится только тем, что парная проверка упала.

**Открытые вопросы:** «Вписать целиком» только для игры — в ячейках коллажа не делал (предложил Денису, ответа не было); «Диагональ» и «Стопка» без ячеек, кадр в них мышкой не двигается; флак `truncate` — один раз отбился CORS боевого `api.poigraem.shop`, повтор 76/76.

**Что осталось:**
1. Денису — после Ctrl+F5 проверить обложки и коллажи живьём (список из 7 пунктов — в конце сессии), плюс висящие ротация и сцены интерьера.
2. Пул приёмов — перенесён на следующую сессию.
3. Прежние хвосты: корзина в бэкапе, раздельный `editShare`, находка №5 2б, GFX-11.
4. **Разбиение `avito-helper.html` (10 114 строк) на модули — решение Дениса: в план, отдельной сессией ДО экспортной версии** (форк проще собрать из модулей, чем копией монолита — тот самый риск дрейфа). Схема: `.css` + ~8 классических `<script>` по модулям (ядро/хранилище, синхронизация, генератор/промпт, сканер, обложки, История, журнал, настройки), без сборщика — Pages работает как есть, функции остаются глобальными. ⚠️ До переноса: метка версии в ссылках на файлы (иначе Pages отдаст новый html со старым скриптом) и `writeBaseline` в `tests/lib.mjs` должен брать из `git HEAD` все файлы, а не только html. Порядок: дословный перенос без правок логики → полный стенд → только потом изменения.

### 2026-09-22 — Ротация объявлений, учёт по аккаунтам, сцены интерьера как данные; 2б подтверждено живьём

**Контекст:** сессия началась с вопроса «выполнен ли план сближения с Айдентикой» и переросла в стратегическую аналитику масштабирования, а затем в две крупные правки. По ходу **Денис подтвердил, что прогнал фото консолей в интерьере — работает**, чем закрыл последнее непроверенное звено направления 2б (у всех прежних проб был `refs=0`).

**Аналитика масштабирования (по прямому запросу).** Разобрано: что у Айдентики лучше, чего нет у обоих, как масштабировать на чужие ниши и как собирать сырьё клиентов. Ключевые выводы: визуал перестал быть преимуществом (Айдентика, Fabula.ai, MPCard.AI — инфографика за 200-800 ₽ у любого), мы в другой категории — поток объявлений против витринной карточки; у Авито нет открытого API объявлений, официальный путь — XML-фид автозагрузки, и клиент, который им уже пользуется, отдаёт весь свой корпус одним импортом; ниши делятся надвое — товарам нужен антидубль, услугам (ноготочки) он не нужен вовсе, им нужны скорость ответа и отзывы, то есть одним продуктом эти аудитории не закрываются.

**Что решил Денис по итогам аналитики:**
- Фичи «быстрый ответ в переписке» и «петля на просмотрах/контактах» — **отклонены**. Про переписку прямо: «я вполне в состоянии сам ответить клиенту… копировать вопрос и вставлять обратно — бредятина».
- **Экспортная версия для клиентов — ОТДЕЛЬНОЕ приложение**, а не сборка из одного исходника. Я предупредил про дрейф двух копий (прецедент Bug #9, два репозитория бэкенда) и предложил сборщик; Денис после объяснения подтвердил решение — форк.
- Свои товары: **техника и гейминг, хобби (без книг), гаджеты, музыкальные инструменты**; одежда и обувь — для экспортной версии.
- Антидубль по сценам отклонён: «я просто сгенерирую новое фото в новой сцене, к чему мне эта опция, только токены жечь».
- Из способов сбора сырья выбрана **голосовая анкета** (оказалась бесплатной: Claude аудио на вход не принимает, но браузерное распознавание речи и диктовка на телефоне дают транскрипт даром).
- ⚠️ **Поправка Дениса, которую принял:** копировать чужие объявления в эталоны можно — запрет на дубли работает внутри своего аккаунта, а чужой текст правилами не ограничен. Моя оговорка касалась только массового автосбора ботом.

**Правка 1 — ротация и аккаунты (`0c84420`).** Объявление выдыхается, и Денис крутит его руками: тот же текст с другими формами написания и новым заголовком. Сделано: **две степени** — 🔁 лёгкая (прокрутка, ~треть слов, структура и факты неприкосновенны) и ↺ средняя (существующий перефраз); крутится **донесённый финал**, а не бот-версия; в промпт уходит список **занятых названий** по товару и аккаунту; «✓ Разместил» заводит новую запись со ссылкой `_rotFrom`, прежнюю переводит в «Снято» и её сырьё не трогает. Порог 14 дней в настройках, свой срок и «не ротировать» у записи, правка даты публикации, сводка «пора обновить» и фильтр. **Аккаунты** — список в настройках (не два зашитых), метка у записи, колонка и фильтр; ручная запись в Истории приняла текст объявления и галочку «заодно в эталоны». Это закрыло дыру: половина работы Дениса (второй аккаунт, объявления без генератора) была вне учёта вовсе.

**Правка 2 — сцены интерьера (`142f356`).** По вопросу Дениса «стоит ли добавить сцен, товары-то разные». Выяснил и сообщил факт: **категория товара в промпт сцены не передаётся**, поэтому привязка сцен к категориям дала бы только удобство выбора — Денис на этом основании выбрал общий список. Сцен стало **18** в четырёх группах, компоновка «частые сверху + Ещё сцены», редактор в ⚙ Настройках (заводские правятся патчем-отличием, как категории). По прямому требованию Дениса добавлены **блоки достоверности**: комнатные сцены просят обычное телефонное фото с бытовыми мелочами, студийные — честную предметную съёмку, обе запрещают рендер, CGI, рекламный глянец и требуют общего освещения товара и фона (иначе предмет читается вклеенным). Это та же отстройка от нейротекстов, только для фото.

**Два круга код-ревью, 16 находок, все исправлены до коммитов.** Тексты — в `AUDITS-ARCHIVE.md`. Самая важная повторяла дефект, закрытый 11.09: список «частых сцен» я положил в `state.settings`, а по отличию настроек от заводских синхронизация решает, можно ли отправить документ на сервер — то есть после первой же генерации картинки пустой телефон затёр бы категории и пресеты, накопленные на ПК. Вторая по весу: аккаунт не расходился между устройствами, потому что миграция проставляет его всем записям до первого pull, и ветка переноса была мертва.

**Новые баги:** не найдено вне собственного кода сессии.

**Стенд:** с 857 до **1067 проверок**, 15 секций (новая `rotate` — 151, `staged` выросла со 101 до 160).

**Открытые вопросы:** мультиарендность (сейчас всё в одном браузере под общим `ADMIN_TOKEN` и на ключе Дениса — продать клиенту нельзя ни технически, ни экономически); как разводить сырьё и журнал экспортной версии, чтобы форк не писал в документы Дениса; видео из фото (цены собраны: 5-секундный ролик $0.25-0.50 на дешёвых моделях против $0.023 за картинку; нужен отдельный маршрут с очередью — буферизовать видео наш процесс не может).

**Что осталось:**
1. Денису — прогнать ротацию на объявлении, которому пора обновиться, и новые сцены (Ctrl+F5).
2. Экспортная версия отдельным приложением: до первого её запуска развести документы синхронизации, иначе она сольёт своё сырьё в `voice` Дениса.
3. Прежние хвосты: корзина и кладбище не входят в бэкап; раздельный `editShare` в анализаторе; пул приёмов и цены каталога (по слову Дениса); GFX-11; косметическая находка №5 (дублирование `stagedToCovers`/`visionToCovers`).

### 2026-09-14 — Пропали финалы: три механизма потери, сырьё восстановлено полностью

**Контекст:** сессия началась с жалобы Дениса — «после обновления пропали финалы, отображается 2, хотя должно быть 4 или 5». Это прямая угроза главной ценности проекта, поэтому всё остальное отложено.

**Итог: потерь нет ни одной.** Финалов было ровно 5, восстановлены все 5.

**Как разбирались (и чем ошибался я):**
- Первая гипотеза — близнецы по контент-ключу — оказалась верной, но я отбросил её на середине: в обоих файлах-бэкапах `trash: 0` и `deleted: 0`, и я прочитал это как «удалений не было». **На самом деле корзина и кладбище просто не входят в экспорт**, а мой скрипт показал отсутствующие поля нулями. Развернуло обратно только сообщение Дениса со статусом синхронизации: «в корзине: 3».
- Вторая ошибка была ценной по-другому: Денис поправил, что в корзине лежат **осознанно удалённые дубли-пустышки**, а пропало нужное. Это и вывело на настоящий механизм — надгробие на общем ключе.
- Вклад VPS-сессии оказался решающим: она нашла каталог `history/`, которого не было в моей задаче, и в нём — версию хранилища от 12.09 14:30 с двумя живыми финалами. Плюс доказала журналом, что вкладки работали внахлёст.

**Три механизма (все уносили записи молча, мимо корзины):**
1. **Близнецы.** Повторные нажатия «✓ Размещено» дают записи с одинаковыми товаром, регионом, названием и датой — то есть с ОДНИМ ключом. Удаление пустого дубля ставило надгробие на общий ключ, слияние уносило по нему запись с финалом. Обе Сыктывкарские.
2. **Затирание из соседней вкладки.** Вкладка A открыта в 10:29 и 5 часов молчала; Пермь создала вкладка B в 13:26; в 16:16 A ожила, разместила своё объявление и записала хранилище снимком трёхчасовой давности. Доказано по `build` (`document.lastModified`) в событиях `app_start`: два разных значения чередуются во времени.
3. **Удаление записи с финалом ничем не предупреждало.**

**Что сделано в коде (`8288376`, стенд 857/857, +32 проверки):** постоянный `_uid` всем записям с миграцией при запуске; разделение ключей на личность и запасной (`SYNC_KIND`, `syncMatcher`); запись сырья слиянием (`saveMerged`) вместо перезаписи; предохранитель `syncKilled` — финал не выбрасывается по надгробию-наследию, но явное удаление уважается; предупреждение при удалении записи с финалом.

**Код-ревью дало 8 находок, и главная была в моём же фиксе:** `syncDbKeys` добавлял контент-ключ всегда, поэтому два близнеца с разными `_uid` считались одной записью — `saveMerged` терял ровно то, ради чего написан, а финал второй публикации приписывался первой. Исправлено разделением сортов ключа. Остальные: замена базы импортом молча сливала старое обратно (`_lsMine` пуст до первого `save`); предохранитель применялся и ко входящим, делая удалённую запись невозвратно-неудаляемой; в `saveMerged` не было предохранителя и перештамповки id; `revived` снимал только одну метку. Попутно вскрылось, что существующий тест «база заменена» **проходил случайно** — миграция успевала заполнить `_lsMine`.

**Восстановление:** две записи собраны из бэкапа 12.09, Пермь — из журнала (`edited` несёт обе версии текста). Длины сошлись с оригиналом до знака, включая маскированное слово «пoдп-cкa» с латинскими буквами внутри — при сборке я его сперва переврал, поймал сверкой длин и посимвольно исправил.

**Серверная часть:** ожидал незакоммиченную правку `syncRotate` в боевом дереве — **не подтвердилось**, она уже в git (`poigraem-backend@d9ef859`, коммит сделан через 24 минуты после отчёта, на который я опирался). Локальная копия подтянута до `68ca927`.

**Что решил Денис:** восстановить все три записи с защитой от повтора; чинить полностью (`_uid` + предохранитель + предупреждение); межвкладочное затирание лечить слиянием при записи; сперва разобрать Пермь, потом чинить.

**Новые баги:** три механизма выше — в коде, существовавшем до сессии. Плюс 8 находок ревью в коде самой сессии, все исправлены до пуша.

**Открытые вопросы:** корзина и кладбище не входят в бэкап (цена подтверждена делом); белый список параметров `/api/image` зашит под одного вендора (записано в `TASK-IMAGE-MODELS-VPS.md`); `~/avito-rescue/` на сервере пока не убирать.

**Что осталось:**
1. Денису — прогнать «🏠 Товар в интерьере» на живом инструменте (единственное непроверенное звено 2б).
2. Продолжать работу и доносить финалы; при ~20 парах — разбор с раздельным счётом тело/хвост/удержание.
3. Прежние хвосты: пул приёмов и цены каталога (по слову Дениса), GFX-11, косметическая находка №5 (дублирование `stagedToCovers`/`visionToCovers`).

➡️ Запись 2026-09-13 (ключ OpenRouter, выбор image-модели, Bug #9 end-to-end) — в `SESSIONS-ARCHIVE.md`, вынесена сессией 25.09.2026.
