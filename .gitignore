# Сборка для ручной передачи файлом — не публикуем в репозиторий
avito-helper-netlify.zip

# Выгрузки журнала самообучения для локального разбора — статистику действий не публикуем
journal/
avito-journal-*.jsonl
