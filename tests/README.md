# Регрессионный стенд avito-helper

Гоняет **настоящие функции приложения** в headless-браузере (Playwright + Chromium).
Заглушки ставятся только на границы: `callAI`, `fetch`, `confirm`, `prompt`, `Math.random`.

## Запуск

```bash
cd tests
npm install                    # один раз
npx playwright install chromium  # если браузера ещё нет в кэше
npm test
```

Выход: список проверок и итог `N/N`. Ненулевой exit code = что-то упало.

## Что внутри

| Секция | Что проверяет |
|---|---|
| `parity` | Промпт (`catInfoBlock` / `buildMsg`), `validateForm`, системный промпт (`buildSys`) и вердикты Сканера (`checkAds`) сравниваются **байт-в-байт** с версией из `git HEAD`. Главная страховка рефакторинга: генерация подписок вылизана десятком сессий, менять её молча нельзя. |
| `editor` | Конструктор категорий: создание, поля, порядок, скрытие, обязательность, сброс к заводской, экспорт/импорт JSON, персист через `reload()`. |
| `stage3` | Категория правит Сканер и SYS одними галочками, штамп товара по флажку «название», инфографика и распознавание по фото собираются из дескрипторов. |
| `phys` | Встроенная категория «Физический товар» целиком: форма, промпт, свои правила Сканера, SYS, инфографика, распознавание, генерация, правка патчем и сброс. |
| `fewshot` | Образцы языка в промпте: избранное + свои эталоны (✍), приоритет своей категории, переток из чужой с пометкой, крючки заголовков физтовара, разбор чужих эталонов (👀) на приёмы и подмешивание приёмов вместо текста. |
| `uncertain` | Уточняющие чипы распознавания: узкий порог (единственный вариант, неизвестное поле, значение вне списка опций и мусор отсеиваются), выбор человека, журнальные пары «предложено → выбрано», сброс при откате формы. |
| `review` | Находки код-ревью этапов 2–4, закреплённые тестом: тумблер не шлёт `false` в промпт, название категории экранируется, очистка полей встроенной категории применяется, инфографика не рисует запрещённую цену, устаревшее значение `select` не уходит в промпт. |
| `journal` | Доставка событий журнала: ключ идемпотентности `eid`, пауза после неудач (назначается, растёт, не пропускает повторных запросов, снимается успехом), отвергнутый батч выбрасывается без роста паузы, без токена запрос не уходит, перепись кликов отсекает занятые кнопки по флагу каждого действия, вкладки рендерятся при недоступном `localStorage`. |
| `staged` | «Товар в интерьере» (2б): промпт запрещает перерисовывать товар и добавлять комплектацию, тело запроса собирается белым списком (модель и `n` клиент не задаёт, соотношение 3:4), терпимый разбор ответа вендора по всем правдоподобным формам с внятной ошибкой при промахе, `503` (нет ключа на сервере) НЕ сбрасывает админ-токен, а `401` сбрасывает, смена фото убирает интерьер прежнего товара, `freshVision()` не роняет карточку после «убрать фото», ничего не попадает в `localStorage`. |
| `staged/сцены` | Сцены интерьера как данные (18 заводских в четырёх группах, у студийных помечен ровный фон), компоновка «частые сверху + Ещё сцены» (использованная поднимается наверх и запоминается в настройках, выбранная всегда видна и ровно один раз), редактор в ⚙ Настройках: своя сцена, правка заводской патчем-отличием и «вернуть заводскую», скрытие заводской флагом (выбранная при этом не остаётся мёртвой), общий сброс; пустой текст сцены не сохраняется и в карточку не попадает; набор переживает перезапуск, входит в бэкап и уезжает на сервер в настройках. Плюс блоки достоверности в промпте: комнатная сцена требует обычного фото с бытовыми мелочами, студийная — честной предметной съёмки, и обе запрещают рендер, CGI и рекламный глянец. Находки код-ревью самой правки: список «частых» сцен хранится ОТДЕЛЬНЫМ ключом, а не в настройках (в настройках он с первой же генерации делал бы их «своими», и пустой телефон затирал бы категории и пресеты на сервере — дефект, уже закрытый ревью 11.09); выбранная сцена переводится на живую при любой пересборке набора, а не только при удалении (иначе генерация уходила бы по первой сцене, а в журнал и имя файла писалась исчезнувшая); «+ Своя сцена» держит черновик в state и не оседает мусором при «Отмене»; последнюю сцену удалить не дают; блок достоверности отделён пустой строкой, а не приклеен к списку запретов. |
| `backup` | Полный бэкап (v3): в файл идёт всё накопленное руками (история, эталоны, избранное, пресеты, очередь, категории, каталоги, настройки, обложки), админ-токен и транзитные результаты генерации — нет; импорт на чистом браузере восстанавливает это и переживает F5, повторный импорт не двоит (дедуп по содержимому, не по id), коллизии id разводятся и ссылка очереди на избранное чинится, отказ от настроек оставляет свои, отмена замены не добавляет ничего, старые форматы файла (голый массив и `{db, refs}`) по-прежнему принимаются, мусорный файл даёт понятную ошибку. |
| `sync` | Синхронизация с сервером плюс 10 находок её код-ревью: объединённый набор уезжает обратно (иначе записи остаются на одном устройстве), финал догоняет уже известную запись Истории, повторно заведённая запись снимает метку удаления, переключение голоса не плодит копий, пустой браузер не стирает настройки на сервере, кнопка отправки не врёт про успех, возврат из корзины, подпись ловит правку той же длины, правка во время отправки не теряется, корзина ограничена байтами. Плюс: подпись документа отсекает лишние отправки, пустой клиент НЕ стирает сырьё на сервере, слияние не двоит и доезжает до диска, удалённое не возвращается с сервера и не участвует в генерации (но сохраняется в корзине), ошибки сети/5xx/битый документ не трогают локальные данные и не шумят в консоль, без токена и при выключенном тумблере запросов нет, настройки уезжают сами, но применяются только после подтверждения. |
| `truncate` | Обрыв ответа модели (живой дефект 12.09 — «Expected ',' or ']' after array element»): бюджет выхода считается от числа вариантов (`genTokens`), `stop_reason: max_tokens` у Claude и `finish_reason: length` у OpenRouter распознаются, из недописанного JSON спасаются целые варианты (скобки и кавычки внутри описания сканер не путает с границей объекта), продавца предупреждают «получено N из M», обрыв виден в журнале, а целый ответ проходит молча. Плюс находки код-ревью самой правки: полный набор, не прошедший строгий разбор (проза с «}» после JSON), не пугает ложным «оборвался»; дыра в СЕРЕДИНЕ спасённого набора не сдвигает выбранные заголовки на чужие описания в воронке; счётчик, переключённый во время генерации, не подменяет число, с которым сравнивают полученное; совет «уменьшите число вариантов» не всплывает на путях без счётчика (распознавание по фото, поиск цены); обрыв попадает в журнал на всех платных путях (`main`, `more`, `regen`, `batch`, `alt_titles`, `descriptions`), а не только в основной генерации. Отдельно шаг «сначала заголовки» (второй живой обрыв 12.09 — `Unexpected end of JSON input`): свой бюджет `titleTokens(n, webSearch)` с надбавкой на tool-use блоки вместо прежних фиксированных 1400, терпимый `parseTitles` берёт из недописанного ответа дописанные названия с их крючками, пустой ответ даёт понятную ошибку вместо ошибки про JSON. |
| `history` | Сохранность записей Истории и финалов с Авито (три живых дефекта 12.09): диалог финала предзаполняется уже донесённым текстом и НЕ затирает его ни пустым, ни очищенным полем, повторное сохранение без правок не двоит событие `edited`; правка ✎ больше не раздваивает запись — у неё постоянный `_uid`, а прежняя версия с сервера гасится меткой; повторное «✓ Размещено» по той же карточке переспрашивает, отказ записи не создаёт, согласие даёт вторую публикацию с отдельной личностью. |
| `rotate` | Ротация объявлений и учёт по аккаунтам: возраст считается от даты публикации (правленая важнее момента нажатия), порог общий из настроек и свой у записи, «не ротировать» и снятое/на паузе из напоминаний выпадают; крутится донесённый финал, а не бот-версия; занятые названия собираются по своему аккаунту (соседний профиль только помечается, если не включено «по всем»); два промпта — лёгкий бережёт структуру, факты и личный хвост продавца, средний требует структурно другого; замок по паре «запись + степень» (двойной тап оплачивается один раз, лёгкая и средняя не мешают друг другу), запись без текста генерацию не тратит, ошибка вендора не залипает замком; «✓ Разместил» ЗАВОДИТ новую запись со ссылкой `_rotFrom`, а прежнюю только переводит в «Снято» — её финал, доля правок и заголовок целы; ручная запись со вторым аккаунтом полноценна (текст, дата, `_uid`), уходит в эталоны один раз и не двоит сырьё при повторе, пустое поле правки сохранённый текст не стирает; записи до этой версии мигрируют на первый аккаунт, переименование и удаление аккаунта их не обезличивают. Плюс 10 находок код-ревью самой правки: битый список аккаунтов (запись без `id`) не роняет «✓ Размещено» и открытие панели; удалённый профиль, оставшийся в черновике формы, заменяется первым, а не пишется в запись меткой «удалён»; явно выбранный на телефоне аккаунт доезжает до ПК (метка `_accAuto` отличает автопростановку миграции от выбора человека, и свой явный выбор входящим не перебивается); снятые «не ротировать» и свой срок хранятся как `false`/`0`, иначе синхронизация воскрешала их с соседнего устройства; очистка ОДНОГО поля описания больше не стирает сохранённый текст (а с ним базу для доли правок финала); заход в соседнюю запись не выбрасывает оплаченный результат ротации; копии из панели уходят в журнал с товаром, категорией, цепочкой и степенью; категорию эталона задаёт человек, а не текущая форма генератора; переименование аккаунта не правит заводскую константу `DEF_SETTINGS`; события `rotate` и пути `rotate`/`rotate_mid` внесены в таксономию `tools/journal-report.mjs`. |
| `layers` | Слои оформления обложки (24.09.2026): прежние стикер, надпись, инфографика и значки платформ переводятся в слои без потерь (выключенное со своим текстом — скрытым слоем, перетащенное — на том же месте, старые ключи из настроек уходят; тот же перевод на входе с сервера и из бэкапа, мусорные слои отсеиваются); у всех шести форм стикера шрифт не мельчает от длины текста, а форма растёт и текст целиком внутри; длинная строка инфографики не режется по буквам, блок влезает за счёт шрифта; лента в каждом углу держит надпись в кромках и отодвигается под длинную; ручки на холсте — белая крутит (прилипание к 90°), жёлтая меняет размер, перенос прилипает к центру с направляющими, колесо над элементом меняет его, а не кадр; рамка выделения не попадает в скачанный файл; Del и «↶ Отменить»; шаблоны — готовые («Предзаказ», «Карточка товара» из формы генератора), свой с фоном и инфографикой «из формы», переживают F5, не раздувают журнал; «4 варианта» не сдвигают сам слой. |
| `letters` | Буквы слоёв (25.09.2026, план Fotor Ф1): старые надписи рисуются **пиксель в пиксель как в `git HEAD`** (страница baseline открывается рядом), стикеры, лента и инфографика 24.09 — тех же размеров, что в HEAD; прежняя «тень без подложки» переводится в явный флаг на всех входах (плоский формат, сервер/бэкап, свой шаблон 24.09), мусорные шрифт/заливка/числа — к заводским; у JetBrains Mono настоящий жирный 700; все шесть шрифтов грузятся с кириллицей и реально меняют ширину; с обводкой форма стикера (все шесть) и плашка инфографики растут, буквы с обводкой внутри; обводка, градиент и тень видны в пикселях, тень отбрасывает контур с обводкой и масштабируется со слоем; меню (блок «Буквы» у стикера, ленты, надписи, инфографики, нет у платформ; «Жирный» только у шрифтов с двумя начертаниями); свой шаблон и F5 сохраняют буквы; скачивание дожидается загрузки шрифта. |
| `collage` | Коллаж по плану Fotor Ф6 (25.09.2026): с настройками по умолчанию коллаж **пиксель в пиксель как в `git HEAD`** во всех видах раскладок (прямоугольные, круг, треугольники, квадрат, полосы, стопка, лента; с неоном и без; живой и вариант); поле по краю отдельно от шва — геометрия ячеек, пиксели поля, «Круг+4» остаётся по центру; скругление 0 и 60, «авто» — прежняя формула; четыре узора светлее тёмного фона и темнее светлого, плотность работает, узор виден и в рамке «Игры»; «Вписать целиком» в ячейке (середина — фото, поля — размытая копия), переключатель только у прямоугольных ячеек и сбрасывает сдвиг; меню поля («как шов» идёт за швом, свой — отключает автомат, возврат); поле, шов и узор переживают F5 (шов раньше не сохранялся); мусор с сервера — к «как было»; свой шаблон и «Отменить» держат поле, углы и узор; вариант «Текущий» — с живым полем. Находки ревью: свой шаблон до Ф6 сбрасывает поле, углы и узор к «как было» (готовый шаблон без фона их не трогает); узор идёт и по кольцу вокруг круга в «Круг+4», и по полям вписанной обложки в «Игре» с тенью. |
| `regress` | Все 6 вкладок рендерятся, генерация по каждой категории, воронка заголовков, пакет по регионам, «Ещё варианты» (уникальность id — Bug #11), перегенерация, сканер, инфографика, распознавание по фото. |

Правило Сканера «аккаунт» с этапа 4 ловит и косвенные падежи (`аккаунтом`,
`аккаунта`, `учётной записи`, `профилю`) — раньше срабатывал только именительный.
У «профиля» окончания перечислены поимённо, чтобы не ловить «профилактику» и
«профильную трубу»; на это есть отдельный тест в секции `phys`.

Базовая версия для паритета берётся из `git HEAD` и кладётся в `tests/.baseline/`
(в `.gitignore`). Если правка сознательно меняет промпт — паритет упадёт, и это
повод осознанно обновить ожидания, а не «поправить тест».

**Как менять промпт осознанно.** В `parity.mjs` есть `EXPECTED_SYS_LINES` и
`EXPECTED_SCAN_LABELS` — явный список того, что мы решили изменить. Строки
оттуда вычитаются из новой версии перед сравнением, всё остальное обязано
совпадать байт-в-байт. **Baseline едет вперёд с каждым коммитом**, поэтому
сразу после коммита изменения эти списки нужно очистить: иначе стенд станет
вычитать строку, которая уже есть и в эталоне. Само поведение закрепляется
обычным тестом в своей секции, а не этим списком.

## Разбор журнала — `tools/journal-report.mjs`

Не часть стенда, но живёт рядом и нужен для месячного разбора журнала
самообучения:

```bash
node tools/journal-report.mjs            # всё *.jsonl из ./journal/
node tools/journal-report.mjs файл.jsonl # или конкретные файлы/папки
```

Читает и серверные месячные файлы (`~/avito-journal/YYYY-MM.jsonl` с VPS), и
локальные выгрузки буфера («⬇ Буфер (JSONL)» в ⚙ Настройках) — формат один.
Печатает 10 секций: целостность доставки (дыры по дням, устройства, дубли,
потери), таксономия событий с явным списком ни разу не встретившихся, воронка
цепочек от генерации до размещения, победный индекс варианта, светофор в момент
копии, `editShare`, качество распознавания по парам «предложено → выбрано»,
раскладки обложек, перепись живых и мёртвых функций, ошибки по стадиям.

Две ловушки, заложенные в код:
- **дедуп обязателен** — по `eid`, а у событий старше 09.09.2026 ключа нет, для
  них остаётся грубый `ts + device + event`;
- **`editShare` считается только по доносам с `descPasted: true`** — если поле
  описания в диалоге осталось пустым, клиент подставляет исходный текст сам, и
  доля правок выходит нулевой по построению, а не по факту.

## Почему стенд в репозитории

Набор писался заново трижды (июль — 101 проверка, сентябрь — 93, этап 1 движка
категорий — 129) и каждый раз умирал вместе с временной папкой сессии. Этапы 3–5
направления «категории-конструктор» опираются на тот же паритет, поэтому стенд
переехал в репозиторий. Ключей и данных продавца в нём нет.
